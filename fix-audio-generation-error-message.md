# 修复音频生成节点错误消息显示问题

## 问题描述

音频生成节点（生成旁白）报错时，显示"图片生成超过限制次数"，这是错误的消息映射。应该显示"音频生成超过限制次数"。

## 问题截图

![错误截图](用户提供的截图显示音频节点报"图片生成超过限制次数")

## 根本原因

错误消息没有根据节点的媒体类型（image/video/audio）动态生成，而是统一使用了"图片生成"的文案。

## 修复方案

### 方案1: 修复错误消息映射（推荐）

在错误消息生成时，根据节点类型动态选择正确的文案:

```javascript
// 在错误消息处理函数中
function getGenerationLimitErrorMessage(mediaType) {
  const typeLabels = {
    image: '图片生成',
    video: '视频生成', 
    audio: '音频生成'
  };
  
  const typeLabel = typeLabels[mediaType] || '生成';
  return `${typeLabel}超过限制次数`;
}
```

### 方案2: 使用通用错误消息

如果难以获取媒体类型，可以使用更通用的错误消息:

```javascript
// 改为通用消息
'生成任务超过限制次数'
// 或
'当前操作超过限制次数'
```

## 需要检查的文件

1. **前端错误消息映射文件**
   - 可能在 `apps/web/src/features/new-canvas/` 或 `apps/web/src/features/production-workbench/` 目录
   - 搜索关键词: `超过限制次数`, `generation.*limit`, `图片生成`

2. **AI节点对话框组件**
   - `AINodeDialog` 相关组件
   - 可能在编译后的 `apps/web/ai-canvas-runtime/assets/` 中

3. **国际化文件**
   - `i18n` 或 `locale` 相关文件
   - 错误码到消息的映射表

## 修复步骤

1. 找到错误消息映射的源文件
2. 添加媒体类型参数传递
3. 根据媒体类型动态生成错误消息
4. 测试三种媒体类型的错误显示

## 测试验证

- [ ] 图片生成超限时显示"图片生成超过限制次数"
- [ ] 视频生成超限时显示"视频生成超过限制次数"  
- [ ] 音频生成超限时显示"音频生成超过限制次数"

## 注意事项

1. 该错误消息可能在编译后的运行时代码中，需要重新编译AI画布运行时
2. 如果使用了国际化，需要同时更新英文、日文、韩文等多语言版本
3. 检查是否有其他类似的硬编码错误消息需要一并修复
