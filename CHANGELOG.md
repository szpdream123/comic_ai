# Changelog

## [Unreleased]

### Added

- 新增灵曦AI GEO 运营闭环：超级管理员可管理问题与公开证据、选择国内主流 AI 平台、聚合多个问题生成结构化草稿，并完成编辑、质检、送审、发布、回滚和归档。
- 新增无需登录即可抓取的 GEO 公开列表与详情页，提供 canonical、结构化数据、证据引用、动态 sitemap 及 robots 索引入口。

### Fixed

- 修复剧集工作台新增角色、场景、道具后因即时刷新读到旧数据而不显示的问题，并统一兼容项目、团队、官方资产库导入记录的 `id` / `assetId` 标识，避免素材卡片无法选中或导入后无反馈。
- 修复版本发布后历史故事板图片因临时签名过期或视频预览字段覆盖而无法显示的问题，改用同项目可用存储对象的稳定鉴权代理地址。
- 修复私有对象存储下团队素材上传、替换与项目素材导入后的访问地址，统一通过鉴权代理读取，避免依赖公开桶 URL。
- 防止并发生成覆盖管理员的新版本或重新激活已归档内容，并阻止同一短链的并发生成互相覆盖。
- 修复 AI 小说转分镜在模型网关持久化连接中断时的错误恢复、主账号与子账号积分返还，以及同一幂等键并发重复生成问题；保留原有 `attemptId` 生产调用契约。

### Changed

- 剧集工作台的生成提示会逐字循环展示“先上传参考图，输入你的想法，再用@引用素材”，输入内容时自动隐藏，并兼容减少动画偏好与窄屏设备。
- 首页推荐内容会从浏览器持久缓存立即恢复，并在首页可见时每 15 秒通过 ETag 条件请求校验后台更新；内容变化后立即更新当前首页。
- 首页背景视频按后台更新时间使用版本化缓存，并支持通过独立环境配置将公开背景视频的 COS 签名与重定向缓存窗口延长至最多 7 天；同版本刷新复用浏览器 HTTP 缓存，新版本 URL 立即失效旧缓存。
- 将对外产品展示名由“灵曦剧场”更新为“灵曦AI”，同步页面、服务端 SEO 输出、无障碍文案和用户指南；内部标识、接口与运行配置保持不变。

## [0.1.0.0] - 2026-08-25

### Added

- 新增 GEO 大模型回答监测：支持导入国内主流平台的完整回答快照，并通过已配置的 DeepSeek、通义官方 API 自动检测品牌提及与官网文章引用。
- 监测结果绑定发布版本，保留不可篡改的原始回答、引用链接与供应商请求归属；后台可查看历史判定和原始快照。

### Changed

- 为官方模型检测增加并发栅栏、心跳恢复、请求硬超时和流式响应上限，避免重复调用、迟到结果写入和异常响应占用资源。

## [0.0.2.0] - 2026-08-25

### Fixed

- GEO 内容中心会在操作时自动补载缺失的详情，编辑草稿、提交审核和发布官网无需刷新页面即可使用；提交审核保留操作时捕获的锁版本，内容被并发修改时仍由服务端拒绝过期提交。

## [0.0.1.1] - 2026-08-21

### Fixed

- 模块导航会先完成页面切换，再按需加载数据；离开页面会取消失效请求，导演台模块会在导航意图出现时预加载。
- 项目列表请求会合并并短暂复用结果，工作流写入后立即失效；重命名和封面修改先更新当前界面，再后台校准服务端数据。
- 认证会话与前端读取接口会安全合并同一并发请求，并隔离不同页面的取消信号，避免重复查询、旧结果回填和跨模块误取消。
- 离开首页时暂停但保留已缓冲的背景视频，返回后复用缓存；Home TV 切换或重渲染时会停止已脱离页面的有声视频。

## [0.0.1.0] - 2026-08-21

### Fixed

- 后台首页推荐视频上传现在会显示上传进度和服务端转码状态，并在超限、网络失败或处理超时时给出明确反馈。
- 首页推荐视频现在可点击播放与停止；切换视频、页面重渲染或媒体播放异常时会正确停止并复位，避免无响应、叠加音频或残留播放。
