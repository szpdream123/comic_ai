# 新画布启动层调试记录

- 症状：进入新画布时短暂显示全屏暗色的 AI Canvas 启动层。
- 根因：宿主挂载运行时时传入 `embedded: false`，运行时因此渲染 `AI Canvas 正在启动` 启动画面。
- 修复：在新画布宿主注入的运行时布局样式中隐藏该启动层，保持运行时初始化和嵌入模式不变。
- 验证：`git diff --check` 通过；相关导航测试中本次新画布路径用例通过，套件另有既有断言失败。
- 状态：DONE_WITH_CONCERNS（完整测试受现有仓库断言失败影响）。
