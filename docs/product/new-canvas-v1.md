# Loomic 独立画布 V1

日期：2026-07-20

## 目标

在现有产品中新增一套独立的 Loomic 画布体验，完整保留画布编辑、素材组织、工作流生成和创作外壳能力，同时不修改现有 `production-workbench/canvas`。

## 实现结论

- 画布内核使用 React 19 和 `@excalidraw/excalidraw@0.18.0`，版本与 Loomic 对齐。
- 画布实现位于 `apps/web/new-canvas`，与现有 X6 画布完全隔离。
- 主工作台“画布”菜单下保留“新画布”入口，继续使用 `/new-canvas/` 路由。
- 图片、视频和音频生成节点已接入后台模型目录、参数映射、任务提交、状态轮询、计费结算、产物归档与画布回填。
- 视频合成节点已接入受控 FFmpeg/ffprobe 执行器和鉴权接口，可把已归档且通过类型化边连接的图片/视频按顺序合成为真实 H.264 MP4，产物上传对象存储并回填画布与运行历史。
- 视频模型后台 schema 中的布尔参数已按真实复选框渲染，`generateAudio` 可原样传入供应商参数以生成视频内嵌音轨。
- 画布文档优先保存在按 `canvasProjectId` 隔离的 IndexedDB 记录中，IndexedDB 不可用时回退到 localStorage；云端画布支持 revision 冲突检测、版本历史与恢复。
- 独立画布项目已接入服务端列表、新建、重命名、切换和删除；每张画布以独立 `canvasProjectId` 作为唯一运行时作用域，离线时仍保留本地草稿能力。

## 访问入口

```text
/new-canvas/
/new-canvas/?canvasProjectId=<canvas-project-uuid>
```

当前重构只接受 `canvasProjectId` 选择具体画布。文档、版本历史、冲突草稿、生命周期草稿、待同步状态和本地对话都按该 ID 隔离；旧 `projectId`、`episodeId`、`canvasId` 参数仅在入口清理时移除，不参与画布运行时、接口归属或产品概念。

## 已具备能力

- Excalidraw 自由画布：选择、绘制、文本、图形、连线、缩放和平移。
- Loomic 工具菜单、图层面板、文件面板和底部画布控制栏。
- 图片、视频和本地素材的画布插入、展示与管理。
- 图片、视频、音频生成节点及其提示词、参考图、画幅、质量、时长、分辨率、文本、音色、语速、声调、音量、后台模型和动态参数面板。
- 工作流模板可一次插入完整的规范节点与类型化连线，并作为一个操作整体撤销。
- 画布标题编辑、Logo 菜单、新建/删除画布项目、撤销/重做和复制对象。
- 创作助手侧栏和本地聊天记录；未确认视觉模型契约前不提供伪多模态附件入口。
- 空画布提示、真实品牌套件面板、保存状态提示和异常兜底。
- 画布内容自动保存、刷新恢复、导出图片和本地项目切换能力。
- 桌面与移动端响应式布局。

## 已完成记录

以下为当前代码与自动化测试已实现状态；仍需生产环境或 headed 浏览器验证的项目以文末限制为准，不以源码检查或内存 mock 代替：

- 画布主壳层已完成，包含工作台导航、画布标题、视图切换、聊天侧栏、文件面板、版本历史与品牌入口。顶栏常驻真实画布标题输入框，读取独立画布项目并通过 `updateCanvasProject` 保存；写入携带 `expectedTitle` 做 compare-and-swap，冲突返回 `canvas_project_title_conflict`。冲突、离线 pending、显式重试/采用云端标题及切换画布前 flush 都按当前 `canvasProjectId` 隔离，不以本地字符串冒充云端成功。
- 左下核心工具已按当前可访问 LibTV 画布的登录态 DOM 与留档截图收口为连续的“资产管理 → 整理画布 → 小地图 → 连接线 → 网格吸附 → 缩放”六项工具组：桌面端资产面板关闭时，资产管理保持首位图标加文字；面板展开后收为与同组按钮等宽的纯图标并切换为“关闭资产管理”动作名。移动端尚无 LibTV 两态证据，继续保留文字和既有横向滚动。其余入口沿用现有真实状态与事件。背景色、图层、自动整理设置和工作流运行控件属于本地已有扩展，保留在核心组后的独立分隔区，不插入 LibTV 核心组、也不冒充其对应控件。中央“教程”入口按 LibTV 提供使用教程、联系客服、联系销售、关注公众号四项菜单，使用项目现有真实飞书手册，客服/销售仅在公开配置返回真实二维码时启用，暂无真实独立配置的公众号明确禁用。
- 空画布启动区已按指定 LibTV 画布逐项对齐：中央提示为“双击画布 / 自由生成节点”，四张快捷卡保持 `214×56` 和 `12px` 间距；中央工具按钮统一为 `32×32`，左下导航按钮为 `28×28`，缩放入口为 `42×28`。四张卡使用 LibTV 原名“故事脚本生成、角色三视图、首帧图生视频、音频生视频”，分别插入脚本到双镜头分镜、角色设定到正/侧/背三张生图、首帧图到视频、脚本到音频再到视频的真实可执行拓扑；每次插入只提交一次 `IMMEDIATELY` 场景更新，可一次撤销。双击画布空白处会在落点打开既有“添加节点”菜单，文本、图片、视频、视频合成、导演台、音频和脚本仍调用原有真实节点创建链；菜单按 LibTV 直接截图分出“添加资源”，上传复用现有导入器，“从生成历史选择”进入真实历史资产层，“素材库”只复用已有风格库与明确禁用的特效库，不新增伪资源。首个导入素材仍按该场景坐标居中；双击已有节点交给编辑器，不误开菜单。
- 对照 LibTV 的底部主工具条补齐了独立“工作流连线”入口；按钮、`Ctrl/Cmd+L`、端口点击和端口拖拽共享同一套类型化连接状态，完成、取消、拖空或源节点失效时同步退出，不使用普通 Excalidraw 箭头冒充工作流连接。端口采用纯视图显隐：空闲且节点未选中时不常驻显示，节点被选中、连线模式开启、已选输出端口或正在拖拽时恢复显示；隐藏端口同时退出指针与键盘可达树，不改连线数据、撤销栈或保存正文。
- 对照 LibTV 的中央入口补齐了“素材库”“角色库”“历史记录”三个按钮，但当前完成边界不同：中央“素材库”已按登录态直接证据改为仅含“风格库 / 特效库”的小菜单，底部“资产管理”继续保持独立；“风格库”只复用现有真实 `project-styles` / `batch image styles` 数据和 `mergeCanvasStylePrompt` 应用链，进入专用风格 dialog，不再跳转资产管理。“特效库”因本项目没有可执行特效节点、加载 API 和供应商任务链而明确禁用，不新增伪页面或静态特效卡。中央“角色库”进入专用 dialog，隐藏通用文件标签和资源筛选，只读取 official/team/project 返回的真实 `character` 资产，以单张真实预览呈现当前角色、底部真实角色缩略图带切换当前项，并由“应用至画布”复用既有稳定对象插入链；没有真实字段的多视图、标签和最近使用不伪造。中央“历史记录”进入专用“历史资产”dialog，不展示通用文件视图标签和节点选择器，固定聚合全部生成节点，只保留“图片历史/视频历史/音频历史”、时间排序、批量操作及右上“− 100% +”；每次进入以图片、时间降序、`100%` 和未开启批量操作呈现，只通过历史产物 CSS 变量调整卡片密度，不读取或写入 Excalidraw 画布缩放。关闭中央层后会恢复左侧紧凑面板进入前的标签、节点、分类、排序和批量状态，左侧“生成历史”继续保留通用导航、节点选择器和默认卡片尺寸。
- 中央移动工具已收口为单一入口，二级菜单真实切换 Excalidraw `selection` 的“移动(V)”与 `hand` 的“抓手工具(H)”；原生 `V/H` 快捷键切换后菜单选中态同步回写。移动端二级菜单通过画布根节点 Portal 展开，并按触发器、实际菜单尺寸、视口边界与全局 UI 缩放夹取位置，避免被横向滚动工具条裁切；打开后聚焦当前项，支持方向键、`Home/End`、`Tab` 和 `Escape` 的连续键盘导航。网格按钮使用 Excalidraw `gridModeEnabled`，实际参与元素吸附和画布持久化，按钮暴露可逆的 `aria-pressed` 状态，不再仅表述为装饰网格显隐。
- 素材导入的前端代码链与自动化测试已完成，覆盖菜单选择、按落点拖放图片/视频/音频、粘贴剪贴板图片、云端资源插入及归档失败兜底。批量导入按受支持文件的原始顺序逐项执行并返回一一对应的成功状态；单张图片读取/解析异常会记录 `false`、提示具体文件并继续后续视频或音频，不会让整批中断、结果错位或改写其他成功项的稳定对象元数据。2026-07-23 已在读取项目 `.env` 正式 `DATABASE_URL` 与 COS 配置的隔离 Chromium 中走通“真实图片文件 → 上传会话 → 对象归档 → 插入画布 → revision 2 保存 → 第二标签实时接收 → 刷新恢复 → 鉴权内容读取”；全程未触发生成请求，临时项目与对象经正式删除接口清理并验证为 `404`。
- 文件与资产面板的本地复用素材、云素材和生成历史产物支持直接拖到画布指定落点；拖拽载荷只保存面板内部 ID，不复制签名 URL，落点按当前缩放和平移换算为场景坐标，原有点击插入和自动定位行为保持不变。云素材和历史产物在异步读取图片或视频前后都会校验当前面板仍打开、画布 API 未更换且 `canvasProjectId` 未切换；关闭资源层或切换画布后才完成的请求不会迟到写入旧场景，本地素材复制也使用同一作用域门禁。
- 节点与连线能力已完成，生成节点、工作流端口、可执行边、节点选择和节点布局规则都已落地。工作流收集器只接受显式 `workflowEdge: true`；新建普通 Excalidraw 箭头会写入 `workflowEdge: false`，不会参与生成计划、依赖指纹或计费。历史无标记但仍满足类型化端口契约的 legacy 箭头只在 hydration 时一次迁移为工作流边，与新边共用同一验证、查找、断开、重连和生成输入来源；改连会升级显式标记并清理旧端点绑定，标记写回不会形成 `onChange` 或保存循环。相同 source/target/port 的重复边在新建时明确拒绝，导入或历史恢复的重复组在断开时一次清理、重连时归一为一条，不增加依赖指纹、生成输入或批量计费节点。持久化、导入或 undo 恢复出的依赖环会在工作流计划和积分提交前以 `canvas_workflow_cycle` 明确阻断并提示断开循环，不再静默丢弃一条边后按错误 DAG 执行。视频合成节点已按 LibTV 直接可见的左右端口补齐右侧输出端口；本地规范端口为 `out_video/video`，该具体端口 ID 与类型来自本项目真实 MP4 产物契约，不冒充 LibTV 内部协议。
- 连接线总显隐已改为纯视图投影：不再写箭头 `customData`、版本号、更新时间或撤销栈；自动保存、`Ctrl/Cmd+S`、生成前保存、生命周期暂存、远端同步、版本指纹和页面级快照统一在序列化前反投影真实箭头透明度。全局隐藏期间新增、重连或由图层恢复的连接会继续保持不可见，图层自身的隐藏状态仍独立持久化。
- 工具预设库已完成：中央工具箱和资源库“工具箱”共用 `canvas-tool-preset-catalog` 真实目录、详情/版本缓存与拓扑插入器，内置和用户预设不会再分叉成两套静态卡片。中央工具箱改为独立可扫描面板，提供分类、真实拓扑预览、三列滚动卡片、“使用”动作和拖到画布指定场景坐标；内置 12 个版本化预设，覆盖脚本生图、脚本生视频、图片转视频、双镜头分镜、角色三视图、导演构图、导演双镜头、单/双镜头成片、脚本配音、音频生视频和导演宣传片。用户可把当前选中的规范子图保存为团队共享工具，刷新和跨会话后继续使用，并可创建不可变拓扑版本、选择精确历史版本、更新、重命名、复制和归档删除；主用户与其子用户共享，跨主用户隔离。列表仅返回摘要时按需加载详情并去重并发请求，切换面板或画布后的 stale 响应不会插入场景。每次点击或拖放插入均生成全新节点/边 ID；插入器先在内存 staging API 完整构建和校验拓扑，再以唯一一次 Excalidraw `IMMEDIATELY` 更新提交，真实撤销 action 会整体移除节点和边，失败不会遗留半组节点。服务端使用独立 `creator_tool_presets` / `creator_tool_preset_versions` 契约，递归拒绝媒体 URL、存储对象、任务、运行结果等不可移植运行态数据，创建/复制具备幂等重放，拓扑更新使用 `expectedVersionNumber` 并发版本校验；不复用媒体资产表，也不使用静态卡片冒充功能。预设只保存规范节点与边拓扑，不保存端口版本；旧预设无需数据迁移即可在插入和重新序列化时采用当前视频合成输出契约。
- 图片/视频生成流程已完成，支持按节点执行、工作流队列、暂停/继续/停止排队、按画布持久化待执行顺序、刷新后恢复运行中/暂停/失败队列、任务标识立即持久化、已提交任务无重复提交续跑、正式任务 `result.imageUrl/videoUrl/sourceUrl` 提取、人工审核/结果未知终态处理、任务结果回填、失败状态保留和产物插入。提交与轮询期间会保存 `generationSnapshot`，`taskId` 首次写入时会同批持久化覆盖节点参数、上游文本、稳定素材和类型化连线的 `generationInputSignature`；同会话或刷新续跑发生指纹漂移时均停止轮询、禁止旧输入和旧产物回写，把远端任务线索转存为 `staleGenerationTaskId` 并进入 `inputUpdated`。若任务提交成功但画布 revision 未保存，其他设备会从该节点最新的服务端 run 恢复：只接受与当前节点、上游文本和稳定素材输入严格匹配的最高 `runNo`，活动任务仅 GET 原 `taskId`，成功 run 直接回填服务端 artifact，终态不自动重试且不回扫旧 run；生成产物按稳定对象 ID/URL 去重，不重复插入画布。生成成功但本地插入失败时保留已付费成功状态和结果 URL，可从生成历史重试。已完成的视频合成节点可作为不重复计费的物化工作流屏障向下游视频节点提供归档 MP4；后端会按当前类型化连线顺序、素材对象 ID、片段时长、尺寸、帧率和图片默认时长重算签名，未完成、输入已更新、缺少稳定对象、MIME 非视频或输入签名不一致时，会在工作流计划和单节点执行入口、状态写入、积分检查及供应商调用之前以 `canvas_video_composition_output_unavailable` 阻断。
- 生成输入状态与失败恢复已补齐 LibTV 的双动作语义：已完成节点的提示词、参考素材、模型或参数变化会显示“输入已更新”，可选择“更新生成”或仅关闭提示；关闭不会清除待更新标记，下游仍不会误用旧结果。失败节点显示真实错误、可“重新生成”或关闭失败提示，关闭仅持久化提示 dismiss；生成历史中的失败 run 保留为无伪产物的可重试记录，重试前校验原节点仍存在、输入和模型可用及当前积分余额，余额不足时不提交队列。工作流失败弹层提供独立“重试失败节点”，按当前画布元素重建请求并把失败节点排在原 pending 队列前；普通继续仍跳过失败节点，删除、失效或已知余额不足会阻断重试。图片、视频、音频和 standalone 提交会先把 `generationIdempotencyKey` 写入节点并保存，再传给真实请求；超时、`TypeError` 或 `5xx` 保留原键和 `generationReplayPending`，用户重试会恢复同一次请求，不重复计费，明确失败才生成新键。刷新后无 `taskId` 且服务端没有匹配 run 的不确定提交会进入可重试失败态，不永久卡在 `running`；工作流队列执行前重新读取当前元素，已删除或不再可运行的节点会暂停并报错，不使用旧快照继续提交。故事板即使保留旧产物预览，也优先显示当前“等待登录/恢复结果、正在生成、输入已更新、生成失败或已取消”状态，并对更新、失败和取消提供真实恢复动作；“待生成”筛选覆盖这些可恢复节点。真实失败任务 UI 本轮仅由代码/定向测试覆盖，未人为制造供应商自然失败样本。
- 生成前积分预检已接入真实模型目录与账户余额：图片、视频和音频节点按后端同一套 `baseCredits`、分辨率价目、视频时长计费和取整规则显示预计积分，余额确定不足时在共享 `executeCanvasNodeGeneration` 边界统一阻断并提示充值，键盘快捷键、工作流队列、工具面板、故事板和历史重试均不能绕过；已有 `running + taskId` 的恢复任务不会重复阻断。成本或余额无法证明时不猜价，保留后端最终校验。批量工作流会逐节点汇总全部可证明成本并在确认框显示总预计积分与余额，确定不足时不会启动队列；任一节点成本未知时明确显示未知数量并以后端为准。本轮未点击任何生成按钮，未产生付费任务。
- 导演节点已完成画布内真实文本执行：通过 `POST /api/canvas/:canvasProjectId/nodes/:nodeId/run` 校验画布归属、节点身份、输入长度和幂等键，调用现有 `TextModelGateway` 生成结构化导演指令，写入 `creator_canvas_node_runs` 的 `text` run 与 `creator_canvas_node_artifacts` 的 `text` artifact；前端将结果持久化到节点并参与下游提示词，支持同步结果、运行历史轮询、失败重试、网络不确定响应幂等恢复和刷新后自动恢复。导演 run 会保存无 URL 的 `recoveryInput`，恢复时只接受最高成功 run 与当前导演要求、模型、上游文本、稳定素材和端口连线严格匹配的结果；旧 run 缺少可证明快照、媒体缺稳定对象或请求期间输入变化时均不自动回填。导演节点自身已纳入依赖过期传播，导演文本结果、结构化结果或输入更新回退发生变化时，相连的图片、视频和音频下游会进入 `inputUpdated`。导演上下文只传文本、节点 ID、素材名称/类型和稳定对象 ID，不发送 URL、`data:` 或 `blob:`，后端测试使用 stub gateway，未触发真实付费模型。
- 音频生成流程已完成真实闭环并按官方契约纠偏：正式模型为 `cosyvoice-v2`，通过 `aliyun_bailian_audio` 协议调用百炼非流式同步 HTTP `/api/v1/services/audio/tts/SpeechSynthesizer`，请求使用 `model + input{text,voice,format,sample_rate,volume,rate,pitch}`，不再使用无官方依据的异步 task/poll 契约。通用任务、BullMQ submit/finalize、outbox、任务快照与恢复均支持 `audio`，同步响应的 24 小时结果 URL 会立即进入标准归档。独立画布从 `/api/canvas/:canvasProjectId/nodes/:nodeId/run` 执行，并按需创建受控 project/episode；接口只接受真实 `node.type === "audio"` 且 `node.data.mediaKind === "audio"` 的节点，非法请求不会创建隐藏业务记录。
- 音频任务计费与归档已闭合：提交前预留积分，成功消费、明确失败释放；模糊网络提交进入 `result_unknown` 并继续保留预留积分，避免免费重试。成功结果必须归档为带 `storageObjectId` 且 MIME 为 `audio/*` 的标准 audio artifact；产物下载使用 120 秒超时、100 MB 上限、MIME 校验和流式硬上限。供应商不支持取消时明确返回 `provider_cancel_not_supported`，不伪报取消成功。
- 音频节点当前真实透传参数仅包含 `text`、`voice`、`format`、`sampleRate`、`volume`、`rate` 和 `pitch`。`voice` 必填并由官方 V2 的 107 个系统音色驱动，默认 `longxiaochun_v2`；文本上限 20000；格式支持 `mp3/pcm/wav/opus`；采样率默认 22050；音量范围 `0-100`、默认 50；语速和声调范围均为 `0.5-2`、默认 1。控件范围、步长和默认值读取模型 schema，不再硬编码 LibTV 的外观数值。模型目录会实时驱动节点卡片与参数面板状态。前端模型目录和后端执行解析器均按 schema 裁剪参数，百炼 adapter 再以固定白名单构造请求；旧节点或第三方客户端传入的 `pause/interjection/intensity/timbre/effect` 不会进入供应商请求。
- 工作流停止已接入真实任务取消：排队任务可原子取消；运行中的 Seedance 视频只有供应商 `DELETE` 明确成功后，才同步把任务、attempt、provider request 和生成快照标记为 `canceled` 并释放预留积分。供应商不支持取消、取消失败或任务已终态时，不伪报成功，节点保留 `running`、任务 ID 和远端仍可能运行/计费提示，刷新后仍可恢复结果。
- 图层与资产管理已完成既有数据操作，支持搜索、类型筛选、重命名、排序、锁定、显隐、组合/取消组合、可折叠嵌套分组树、组级选择/移动/锁定/显隐/删除、全部折叠/展开、元素/整组拖拽排序、跨组移动、拖入/拖出分组和从画布删除单项素材/节点；分组名称持久化在组成员元数据中，支持双击或按钮重命名，拖入已命名组会继承名称，拖出或取消组合会清理失效名称映射。搜索或类型筛选只裁剪展示树，组级选择、移动、锁定、显隐、删除和拖拽仍按完整嵌套组路径作用于全部成员，不会在刷新后留下未命中的残余成员。拖拽会保留嵌套组并阻止递归分组，删除节点或组时同步清理工作流连线。个人素材库会根据服务端分页元数据加载全部页面，每批最多并发 4 页并按云素材 ID 去重；部分页面失败时保留已加载素材并提示具体失败页数。2026-07-23 在当前账号可访问的 `spaceId=224879&projectId=cf39ecbc53a34beab3d68c3965ddd4df` 同一可控登录会话中分别切换页签并留档：`artifacts/libtv-current-canvas-tab-2026-07-23.png` 直接确认“画布”页含“画布元素”、排序、“全部”筛选、搜索入口、完整非连线节点列表及固定底栏“共 16 节点”与收起按钮；`artifacts/libtv-current-assets-tab-2026-07-23.png` 直接确认“资产”页为“个人 / Agent”分段、搜索框、单一筛选图标及文件夹“暂无资产”空态；`artifacts/libtv-current-canvas-row-menu-2026-07-23.png` 直接确认至少图片行悬停显示定位入口与三点菜单，菜单项为“重命名 / 复制 / 下载”。这些截图只证明可见结构与该图片行命令，不外推所有节点类型、删除语义或后端契约。本地已按该结构收口：画布页只读投影图片、视频、音频、文本、脚本、导演台、视频合成及其他真实画布元素；画布媒体行保留独立定位入口，并以三点菜单复用真实重命名、媒体复制和下载链，文件夹移动与删除保留在分隔后的本地管理区；个人资产和 Agent 复用原有真实加载与操作链，右侧图标调用现成专用 dialog，上传和批量操作仅保留在该专用资产管理大层，资源库与生成历史仍由中央独立入口打开，不删除功能。
- “画布元素”标题旁的可见排序入口已按 LibTV 截图补齐：本地支持前景在前/背景在前的可逆显示排序，只反转筛选后的列表投影，不改 Excalidraw 场景层级、撤销栈或保存正文。LibTV 截图未证明其内部排序字段或持久化边界，因此不据此改写画布 z-order。
- 云资产管理已补齐真实写操作：团队、项目和剧集资产按既有 API 契约支持重命名/删除，个人与官方素材保持只读；团队子账户仅显示后端允许的重命名，不显示明确禁止的删除，管理员 DELETE 仍是显式审计 override；失败时原列表不变并提示，删除云资产不会删除画布中已有引用，不伪造不存在的云文件夹移动能力。图片、视频和音频本地导入在归档、读取、解码和时长探测的异步边界都会复核当前画布 API，画布切换或组件卸载后不再向旧场景写入或弹出旧提示；云资产重命名/删除的成功、失败和收尾同样绑定当前 API、画布、项目、剧集与面板会话 token，迟到响应不会污染新列表、错误或 busy 状态。项目/角色资源归一化会保留顶层、版本或版本元数据中的稳定 `storageObjectId`，插入画布、复制、保存和跨设备 hydration 不再只依赖可能过期的展示 URL。删除项目资产时，当前画布和 revision 历史仍引用的 `storageObjectId` 保持 `available`，只有无任何画布引用的对象才删除；删除剧集文件资源时，纯画布当前文档或历史 revision 引用会删除资产版本记录但保留存储对象。删除项目时排除被删除项目自身的画布引用，再保留其他仍活动画布引用的对象并解除项目归属，源项目独占且无其他引用的对象才会清理；源项目自身节点 artifact 若指向待清理对象，会先软删除并解除 `storage_object_id`，避免远端对象已删后数据库因 artifact 外键失败而形成半删除。
- 资产页的创建与批量管理代码链已补齐并由自动化测试覆盖：支持一次选择多张图片、视频和音频，复用现有画布导入与对象归档链逐项上传，单项失败不会中断其余文件，完成后重新加载个人素材库；批量模式支持对当前筛选结果全选、按原顺序插入，并以一次 `IMMEDIATELY` 场景事务把本地复用资产的 `canvasFolder` 分类写入画布文档，刷新后继续保留。云资产没有统一可写分类接口，因此仍保持来源库分类并明确提示，不伪造不存在的服务端云文件夹能力。本轮 headed 页面只确认了上传、批量入口和真实个人素材列表，没有实际选择用户文件、上传或下载云资产。
- 云资产插入与下载已统一使用稳定对象内容路由：存在 `storageObjectId` 时始终生成编码后的同源 `/api/storage/objects/:id/content`，不会优先使用过期签名 URL；下载按钮按真实可解析内容 URL 显示，JPEG/WebP/GIF/AVIF/SVG、MP4/WebM/MOV、MP3/WAV/M4A/AAC/FLAC/OGG/Opus 会保留或补齐正确扩展名。视频和音频只有图片缩略图、没有原文件或稳定对象时不会把缩略图误作媒体插入或下载。
- 个人素材库的服务端汇总和分页列表已纳入 `audio/*`，支持 `media=audio` 筛选并返回兼容新增的 `audioCount/audioBytes`；音频条目序列化为 `mediaKind: audio`，归档后的上传音频和生成音频已具备通过稳定对象在其他已登录设备重新读取并插入画布的代码与 HTTP/模块测试路径，但两台真实设备生产联调仍未完成。个人汇总与列表只返回 `storage_objects.status = available`，`pending_upload/failed/delete_failed` 不会被统计或插入；管理员资源审计接口的异常状态可见性保持不变。
- 导演 Agent 资产集合已完成真实持久化：新增 `creator_agent_assets` 表和正式迁移，按当前主用户聚合，主账户与其子账户共享同一集合并记录创建成员，其他主用户不可读取或修改；鉴权 API 支持列表、新建、编辑和软删除，名称唯一、字段长度和 UUID 均有服务端校验。文件面板新增 Agent 视图，可搜索、创建、编辑、删除并把资产插入为真实可执行导演节点；删除集合资产不会删除画布中已经插入的节点配置。
- 品牌套件已按 Loomic 多套件契约完成真实闭环：新增主用户归属的 `creator_brand_kits`、类型化 `creator_brand_kit_assets`（`color/font/logo/image`）和项目 `brand_kit_id`，主账户与子账户共享、跨主用户隔离；支持列表、详情、新建、编辑、默认套件、复制、删除、资产 CRUD 和项目选择，项目查看/编辑能力分别保护读取与绑定。Logo、图片和可选字体文件按主账号对象作用域上传，不绑定某个来源项目，只保存经过归属、available 状态、MIME 与扩展名校验的 `storageObjectId`；因此同一主账号的其他项目和子账户能继续签名读取。读取时生成短期签名 URL，不把签名 URL 写入数据库；存储修复任务会把品牌封面和品牌资产引用计入保留集合，不会在绑定 15 分钟后把仍在使用的文件误判为悬空对象。
- 品牌套件已进入真实画布和生成链路：品牌面板可管理颜色、字体、Logo、参考图片和指南，把 Logo/图片插入画布，把主色与支持的字体映射应用到新建或选中元素，并把 background 角色颜色应用为画布背景；项目当前品牌套件的主上传字体使用 Excalidraw 保留的 custom font slot 和浏览器标准 `FontFace` 真实渲染到新建或选中画布文字，运行时优先从稳定对象鉴权内容路由加载，不依赖短期签名 URL，刷新和其他已登录设备可重新注册。项目选中的品牌指南会去重追加到图片、视频和导演请求，且只发送资产 ID/类型/名称/角色等白名单元数据，不发送签名 URL。音频节点明确不注入品牌文本，避免 TTS 念出颜色和 Logo 名称。
- 失败素材恢复的代码与自动化回归已完成：图片、视频或音频缺少源文件、归档失败或需要重新选择文件时，可校验同媒体类型后原位重新上传并更新稳定 URL，不创建重复节点。此前 headed 原位重绑对象随后又被旧 repair 路径删除，当前修复只有自动化回归证据，因此不把它记为当前稳定的真实上传 headed 闭环。
- 上传归档保留与旧节点恢复的代码和自动化回归已闭合：存储修复任务对已完成超过 15 分钟的上传对象执行悬空清理，但 `project_upload_records` 中状态为 `uploaded`、有完成时间且无错误的正式上传记录，以及画布 revision 中仍引用对应 `uploadSessionId` 的媒体，都会作为保留根，不会被误删；`delete_failed` 等删除失败记录进入可再次修复的持久状态，活动画布引用始终优先保留。原生图片后台归档在上传完成、回写场景前会重新校验画布作用域、当前 `fileId` 二进制 `dataURL` 和 live 节点仍处于本地待归档状态；期间删除节点、重绑源文件或切换画布会把迟到结果标记为 stale，不复活节点、不覆盖新 `storageObjectId`、不向其他画布写入或安排跨画布重试。旧画布加载时会按归档媒体元素的 `uploadSessionId` 去重预检（图片、视频、上传音频；默认并发 4，最多 8），仅把上传会话 `failed/expired` 或存储对象 `deleted/failed` 的确定终态标记为 `cloudArchiveStatus: failed`、`archiveRetryState: needs-file`、`requiresSourceFile: true`；鉴权、网络和其他暂态请求失败不会改变本地节点。标记会通过正常立即保存路径持久化，文件面板显示“重新选择源文件”，用户可在原节点上按媒体类型重绑并继续使用；真实已登录后端的稳定上传、刷新读取和 repair 后持续可用性仍待 headed 验证。
- 聚合生成历史已完成，支持全部生成节点或单节点查看、图片/视频/音频分类计数、时间升降序、批量插入、历史结果回填画布和按所属节点设置当前结果；音频历史切换当前结果会同步更新画布音频节点的结果 URL、MIME、稳定对象 ID 和运行归属，不再只更新服务端选择状态。
- 生成媒体稳定来源已完成，图片/视频产物记录 `storageUrl`、来源和归档状态；上传视频/音频按纯输出素材节点连接，不再错误接收生成输入。
- 已归档图片复用链已修复：个人/项目素材、生成历史和当前结果会保留后端 `storageObjectId`，再次插入画布时继续写入图片元素。连接视频合成节点后会被识别为已归档输入，不再要求用户重复上传或错误禁用合成。
- 跨设备媒体 hydration 与生成入队的代码和自动化闭环已完成：画布元素、图片文件、视频/音频媒体和生成结果以稳定 `storageObjectId` 为主键，过期签名 URL 会在初次读取、跨标签远端 revision 采纳、云端冲突版本接受和历史版本恢复时统一改写为鉴权的同源内容路由（`/api/storage/objects/:storageObjectId/content`）；历史版本恢复后保存的最新正文也使用 hydration 后的稳定路由，不要求再刷新一次。未登录、跨用户或非 `available` 对象不会泄露内容。生成提交前后端按当前用户和对象归属重新签发短期供应商 URL，同时保留对象 ID；幂等指纹基于未签名的稳定输入，不因签名 URL 轮换产生重复付费任务。该项不替代文末尚缺的两台真实设备生产对象联调。
- 缩放控制与基础导航已完成，底栏按 LibTV 收口为单一百分比入口；菜单内提供可编辑缩放值、`Ctrl/Cmd +`、`Ctrl/Cmd -`、`Ctrl/Cmd 0` 和 50/100/800% 预设，`Ctrl/Cmd+0` 在菜单和快捷键面板统一显示“适合屏幕”。小地图默认关闭，桌面空间充足时从底栏小地图按钮正上方展开，空间不足时自动切到下方避让，并支持点击/拖动导航；缩放前后的视口中心场景点保持不漂移。中央工具条按带全局 UI 缩放补偿的视口中心固定，不再随左右侧栏漂移；窄桌面打开左侧工作区时底栏上移避让中央条，全部底栏动作保持可点击。缩放按键不再依赖 Excalidraw 的隐式默认监听，而是与底栏共用 10%–800% 边界和中心锚定换算；小地图在一次拖动手势中冻结按下时的场景映射，视口自身移动不会导致落点坐标连续漂移，并排除 `loomicHidden` 隐藏节点，避免隐藏内容进入投影或压缩比例。“适合屏幕”在底栏菜单、`Ctrl/Cmd+0` 和整理后视口聚焦三条路径统一排除已删除及 `loomicHidden` 隐藏元素，不再因 Excalidraw 默认全场景集合把视口拉向隐藏内容。整理画布成功后按 LibTV 显示“是否保留此次整理结果？ / 还原 / 保留”，还原会恢复整理前的节点几何与视口，保留只确认当前结果；确认期间按钮、设置入口和快捷键都不会覆盖唯一还原快照。旋转矩形类节点的小地图投影和自动整理统一使用绕中心旋转后的轴对齐可视包围盒，并保留 Excalidraw 负宽高元素的真实中心；自由绘图、装饰图形和隐藏元素是否参与自动整理的边界仍列在“尚未闭合的能力”。
- 自定义快捷键子集已收口到同一模块并由面板与显式监听共用：`Ctrl/Cmd+G`、`Ctrl/Cmd+Alt/Option+G`、`Ctrl/Cmd+Shift+G`、`Ctrl/Cmd+D`、`Ctrl/Cmd+L`、`Ctrl/Cmd+Enter`、`Ctrl/Cmd +`、`Ctrl/Cmd -`、`Ctrl/Cmd+0`、`Tab` 与 `Alt/Option+Shift+F`。撤销、重做、删除、Space/中键/双指、`V/H` 等仍由 Excalidraw 原生交互提供，`V/H` 继续显示在真实“移动”工具菜单；快捷键总览当前采用不含 `V/H` 的 LibTV 证据变体，但历史截图另有明确包含“移动 V / 抓手工具 H”的变体，原因尚不能由侧栏、缩放或版本唯一解释，待同一在线会话复核后再决定总览是否变化。`Ctrl/Cmd+S` 与 `?` 也保留各自页面级监听。因此不再宣称所有面板条目与实际监听来自单一契约。不再保留会抢占直线、编辑或原生画布行为的裸 `G/L/D/Enter/0/N`；面板按 LibTV 的创作、缩放、移动画布、其他四栏布局展示，其中“移动画布”当前只列不含 `V/H` 截图直接证明的键盘、触控板、鼠标和整理画布，移动端回退为单栏。`Tab` 与底部“添加节点”按钮复用同一菜单 toggle，输入/文本编辑目标和故事板视图不会响应隐藏工作流的添加节点快捷键。双击空白画布从落点打开同一菜单；底部按钮或 `Tab` 打开时仍按当前画布缩放和平移计算空画布中心，已有节点时放到最右节点旁。
- 工作流切换到故事板时会卸载节点参数编辑器、添加菜单、移动菜单、工具箱、快捷键面板、绘图菜单、端口层、图层、小地图和底栏，复位工作流连接模式及活动绘图/移动工具，并禁用除保存外的工作流专属快捷键；切回工作流不会恢复切换前的浮层或连接/绘图状态。故事板不会接管全局图片粘贴或文件拖放，输入框、文本域、选择框、`contenteditable` 和 `role=textbox` 也不会把媒体输入重定向到隐藏工作流；切换故事板会清除拖放激活态且不渲染导入遮罩。故事板卸载这些浮层不影响页面级 `Ctrl/Cmd+S`、`pagehide`、`visibilitychange` 和组件 `unmount` 的 flush，待保存正文、标题和生命周期草稿仍会进入同一串行保存链。画布选中态仍通过纯视图 `captureUpdate: "NONE"` 清空，不删除节点、不制造撤销记录或额外 revision。
- 修饰键拖动复制已区分 LibTV 的两套内部语义：`Alt/Option+拖动` 只复制节点/分组子节点，不复制边；`Ctrl/Cmd+Alt/Option+拖动` 在单节点时复制全部上游边到新副本，在多选时只复制选区内部边，不复制下游或跨选区边。受控路径恢复原节点 ID、位置和原连接，新副本 ID 随拖动落点移动；锁定节点、未达 4px 阈值和取消拖动不会产生副本，修复投影使用同一 Excalidraw 历史事务。
- 保存与恢复主链路已完成，支持本地/云端保存、保存请求串行化、离线待同步和联网自动重试、页面级 `Ctrl/Cmd+S` 立即保存正文与待同步标题、生命周期恢复、较新关闭草稿保护、冲突处理、最新云端版本接受、冲突草稿历史、版本恢复立即持久化和刷新后恢复。接受云端冲突版本改为“先投影成功、再清冲突并提交本地基线”，投影失败时仍保留本地草稿、待处理冲突和非 saved 状态；版本恢复改为先保存当前快照、应用目标、再提交目标，应用或提交失败会回滚画布与保存基线。版本历史面板按 25 条分页并通过 `beforeRevision` 游标继续加载，服务端列表保持单页最多 100 条并返回 `hasMore/nextCursor`，第一页才合并本地冲突快照且追加页去重排序。离线编辑刷新后仍保留待同步标记，重新联网时若云端 revision 已变化，不会用旧云版本静默覆盖本地草稿，而是进入明确冲突选择。页面可见、在线且本地无脏数据、保存或重试任务时，每 5 秒通过鉴权的 current-head 接口读取真实最新 `serverRevision + document`，不再依赖稀疏版本历史。采纳远端元素、文件、背景、网格和主题时保留本地视口与选区，并以短事务保护和内容指纹抑制中间事件与 echo-save。远端采纳的 revision 二次校验、状态变更和本地缓存写入与本地保存共用同一 `saveChain`；若前置的历史恢复或导航预保存已产生 revision 冲突，排队中的远端采纳会返回失败且不投影、不覆盖本地冲突草稿。
- 顶栏保存反馈已按 LibTV 的稳定态视觉证据收口：只有最终 `saved` 状态从视觉布局中隐藏，但仍保留挂载的 `role=status` 与 `aria-live` 文本供辅助技术读取；初始化、本机待同步、保存中、重试、冲突和失败继续明确可见并保留原重试动作。该改动不调整保存状态机、本地草稿、云同步或冲突契约。
- 顶栏失败重试动作已覆盖正文和项目名两条保存链：正文保存自动重试耗尽、项目名保存失败或两者同时失败时均显示同一个可点击入口；正文通过带当前 `canvasId` 的独立事件调用既有 `persistCurrentCanvas`，项目名继续调用原 `flush`，不会复用 `Ctrl/Cmd+S` 通知而递归或双写。显式顶栏重试和 `Ctrl/Cmd+S` 会先取消旧自动重试计时器、重置尝试次数并保存当前场景，避免旧 pending 对象随后形成重复 revision；冲突状态仍保留原冲突处理，不误显示普通重试。
- 图层重命名的 `Escape` 取消链已补齐：外层面板的捕获监听在普通图层或图层组重命名输入上让事件进入输入控件，由输入控件取消编辑；不会先关闭整个图层面板，也不会触发失焦提交。生产构建的独立 Chromium 已分别验证普通图层和图层组：输入退出、原名称保持且面板继续打开，截图为 `artifacts/new-canvas-layer-rename-escape-2026-07-23.png`。
- 小地图已与图层隐藏语义对齐：`customData.loomicHidden === true` 的节点不再进入小地图节点列表、范围计算或比例换算，远处隐藏内容不会压缩可见节点投影；删除元素和箭头的既有过滤保持不变。导航专项新增远端隐藏节点回归，修复前可稳定复现隐藏节点进入模型，修复后 31/31 通过；生产构建的独立 Chromium 通过真实“文本节点 → 图层隐藏 → 打开小地图”链验证节点矩形由 1 个变为 0 个，截图为 `artifacts/new-canvas-minimap-hidden-filter-2026-07-23.png`。
- 底部资产工作区的“资产”页已按登录态 LibTV 证据把常驻筛选区收口为“搜索框 + 单一筛选图标”；原有真实类型/来源筛选器移入同一非模态浮层，支持再次点击、点击外部、`Escape`、关闭面板或切换页签收起，键盘 `Escape` 关闭后焦点回到筛选触发器，专用资产管理大层和数据读取链保持不变。“画布”页使用“全部”类型筛选与搜索入口，不与资产页共用同一常驻控件形态。图片/视频生成节点标签同时直接消费既有 `inputUpdated` 字段：输入变化时显示“输入已更新”，仍保留分辨率、比例和时长规格；警示色按深浅主题分别保持可读对比度，标签是纯视图投影，不写回节点、不触发请求或保存。
- 初始保存态与冲突恢复已修正：页面先保持 loading，待本地/云端初始化及同步标记读取完成后再挂载可交互画布；初始化失败显示 error，不把尚未判定的状态误报为“已保存”。恢复的 `cloudPending` 草稿进入 local，并在编辑器 hydration 后重新进入串行保存队列和联网重试，不要求用户再编辑一次才续传；恢复的待处理 revision 冲突进入 conflict，标题待同步也会合并进 local/error/conflict；明确接受云端或覆盖后才回到 saved。
- 保存调度已增加完整可持久化内容去重：保存状态变化或 Excalidraw 对相同场景重复触发 `onChange` 时不会重复排队，也不会取消首次真实保存。独立 Chromium 中新增文本和删除文本两次真实编辑只产生两次 `PUT`，4.5 秒后保持“已保存”，未再出现保存循环。
- 离线同步标记已增加可验证的已同步内容指纹：正常云保存保留 `cloudPending=false + contentFingerprint` 基线；若 pending 标记写入失败而正文已经落地，重载会发现指纹不一致并进入冲突而不是静默覆盖；首次没有本地正文时仍直接采用云端，正常 clean 本地缓存更新时不会被误判为冲突。
- 画布已补最小真实在线通知闭环：`GET /api/canvas/:canvasProjectId/live` 使用现有登录 Cookie 和画布查看权限建立长连接 SSE，连接/断开产生真实 presence 快照；成功提交新 revision 后服务端立即向同一画布频道广播 revision，并通过 `.env REDIS_URL` 的 pub/sub 跨实例转发，前端仅在本地无脏数据、无保存/重试任务时复用既有 `checkForRemoteUpdate/adoptRemoteUpdate` 安全采纳，5 秒 revision 检查继续作为断线兜底。Presence 仍只在单服务进程内维护，不显示在线成员或远端光标，也不宣称 CRDT 合并或完整多人实时协作。
- 独立画布项目标题已纳入可靠保存：标题 PATCH 使用串行队列和 `expectedTitle` compare-and-swap，陈旧标题返回 `409 canvas_project_title_conflict + details.currentTitle`；冲突后冻结排队写并提供“使用云端标题/保留本地并覆盖”两条显式路径，失败继续保留按画布隔离的待同步草稿和重试入口。切换项目或离开画布前会等待标题同步，不再静默吞掉失败并显示虚假的“已保存”。
- 画布菜单已接入真实 PNG 导出：过滤已删除元素，调用 Excalidraw `exportToBlob`，创建临时下载链接并在成功或失败路径回收 object URL；空画布和浏览器不支持时明确提示，不伪报下载成功。
- 登录过期恢复链路已完成：新任务提交前遇到 `401/unauthenticated` 会打开现有共用登录框并保持节点可重试；已提交的付费任务在轮询时登录过期会保留 `running`、任务 ID 和待恢复标记，密码登录成功刷新后只轮询原任务，不会重复提交。已确认取消的节点不会恢复轮询，取消失败而脱离本地轮询的远端任务会在刷新后继续恢复。
- 生成任务、历史与当前结果一致性已完成：用户取消会记录为 `canceled` 运行并同步画布节点，不创建伪产物；项目素材的取消、失败、人工审核或结果未知重试在没有新产物时，不会覆盖之前已成功的当前预览和生成结果。
- 图片和视频生成入口已补服务端规范节点类型校验：图片任务只接受持久文档中的 `type === image`，视频任务只接受 `type === video`；非法节点在创建隐藏项目/剧集、节点运行、生成任务或计费记录前返回明确 `400`，避免错误节点触发付费任务。
- 视频合成主链路已完成：合成节点只收集通过类型化工作流边连接的图片/视频节点，运行前要求每个输入已有 `storageObjectId` 且画布云保存成功；后端重新校验画布归属、source/target 端口、edge kind、目标端口 accepts、媒体类型、对象归属和项目归属，不接受客户端 URL 或服务器路径。媒体执行器支持图片/视频混排、逐片段时长、缩放留边、帧率统一、短视频末帧补齐、顺序拼接、H.264、`yuv420p`、`faststart`、`ftyp + ffprobe` 双重校验、路径白名单与 realpath 防逃逸、数量/总时长/尺寸限制、512 MB 单文件和 1 GB 聚合下载限制、60 秒下载超时、输入输出大小限制、临时目录清理和目标不可覆盖。
- 视频合成可靠性已完成：接口使用持久化幂等记录，同一请求键不重复执行或上传，处理中返回 `202`，同键不同请求返回 `409`，明确失败会持久化失败终态并可重放而不会卡在 `processing`；画布在运行前持久化 `running + compositionRequestId`，网络丢响应、超时、`5xx` 和 `idempotency_processing` 保留原键，刷新后可“恢复合成结果”；明确 `4xx/422` 才清活动键并允许新请求，成功后记录 `lastCompositionRequestId`，用户主动重跑才生成新键。成功产物挂载、运行完成和幂等成功在同一数据库事务收口；收口失败会删除刚上传的云对象并把存储记录标为已删除。产物写入 `creator_canvas_node_runs`、`creator_canvas_node_artifacts`，节点可预览并插入可复用的已归档视频元素。
- 独立画布项目已完成真实前后端数据闭环：每张画布拥有独立 `creator_canvas_projects` 聚合根、document、revision、nodes、edges、run 和 artifact。后端接口支持列表、新建、读取/保存、重命名、复制和软删除，所有运行与结果回写都以显式 `canvasProjectId` 校验；复制只复制持久文档、节点、边和稳定素材引用，不复制任务、运行、供应商请求或计费状态。前端通过 URL `canvasProjectId` 选定画布，文档、历史、聊天、冲突、生命周期和待同步本地键均按画布隔离；切换、新建、复制和新窗口前会等待当前画布云保存，冲突、离线待同步或保存失败时阻止导航。
- 节点运行历史的并发编号分配已修复：相同节点的不同幂等请求并发创建时，以唯一索引仲裁并重新读取 `MAX(run_no) + 1`，冲突后有界重试；相同幂等键仍只复用同一运行，不同幂等键会稳定得到不同 `runNo`，避免导演、图片、视频、音频或合成任务因运行编号竞争丢失。
- 正式迁移 `20260720-correct-cosyvoice-v2-contract.sql`、`20260720-enable-project-multi-canvases.sql`、`20260720-create-creator-agent-assets.sql` 与 `20260720-create-creator-brand-kits.sql` 已精确应用到 `.env` 的正式 `DATABASE_URL` 并写入迁移账本。新增迁移执行时整体 runner 先因既有 baseline checksum 漂移停止，随后仅对对应新迁移进行事务化应用和登记，没有修改历史 checksum 或重放 baseline；正式库已确认 Agent 表、品牌套件/资产表和迁移账本记录存在。当前 active 音频模型为 `cosyvoice-v2`、`sync`、`aliyun_bailian_audio`，`poll_queue_name` 为 `NULL`，结果进入 finalize 归档队列。旧业务项目绑定字段不属于当前独立画布运行时契约。

## 已验证

- npm run build:new-canvas
- `$files = Get-ChildItem apps/web/tests -Filter 'new-canvas*.spec.mjs' | Sort-Object FullName | ForEach-Object { $_.FullName }; node scripts/run-tests.mjs $files`
- node scripts/run-tests.mjs apps/backend/src/modules/project/tests/director-canvas-node-run.service.spec.ts apps/backend/src/entrypoints/tests/phone-auth-dev-server.director-node.spec.ts apps/backend/src/modules/project/tests/creator-canvas-record.service.spec.ts
- node scripts/run-tests.mjs apps/backend/src/modules/project/tests/agent-asset.service.spec.ts apps/backend/src/entrypoints/tests/phone-auth-dev-server.agent-assets.spec.ts apps/web/tests/new-canvas-files.spec.mjs scripts/migrate-user-scope.test.mjs
- node scripts/run-tests.mjs apps/backend/src/entrypoints/tests/phone-auth-dev-server.media-library.spec.ts apps/web/tests/new-canvas-files.spec.mjs apps/web/tests/new-canvas-ports.spec.mjs
- node scripts/run-tests.mjs apps/backend/src/entrypoints/tests/phone-auth-dev-server.canvas-live.spec.ts -- --test-name-pattern "latest current canvas head"
- node scripts/run-tests.mjs apps/backend/src/modules/project/tests/canvas-live-collaboration.service.spec.ts apps/backend/src/entrypoints/tests/phone-auth-dev-server.canvas-live.spec.ts（3/3 + 2/2）
- node scripts/run-tests.mjs apps/backend/src/entrypoints/tests/phone-auth-dev-server.canvas-node-type-validation.spec.ts apps/web/tests/new-canvas-layers.spec.mjs apps/web/tests/new-canvas-file-persistence.spec.mjs
- node scripts/run-tests.mjs apps/backend/src/modules/project/tests/brand-kit.service.spec.ts apps/backend/src/entrypoints/tests/phone-auth-dev-server.brand-kits.spec.ts apps/web/tests/new-canvas-brand-kit.spec.mjs
- node scripts/run-tests.mjs apps/backend/src/modules/project/tests/brand-kit-storage-retention.spec.ts
- node scripts/run-tests.mjs apps/backend/src/modules/new-canvas/tests/new-canvas-assistant.service.spec.ts
- node scripts/run-tests.mjs apps/backend/src/entrypoints/tests/phone-auth-dev-server.spec.ts -- --test-name-pattern "protects the new canvas assistant route"
- node scripts/run-tests.mjs apps/web/tests/new-canvas-upload-recovery.spec.mjs（3/3）
- node scripts/run-tests.mjs apps/web/tests/new-canvas-file-persistence.spec.mjs（16/16）
- node scripts/run-tests.mjs apps/web/tests/new-canvas-generation.spec.mjs apps/web/tests/new-canvas-cloud-persistence.spec.mjs apps/web/tests/new-canvas-file-persistence.spec.mjs apps/web/tests/new-canvas-files.spec.mjs
- node scripts/run-tests.mjs apps/web/tests/creator-api.spec.ts（57/57）
- apps/backend/src/modules/project/tests/creator-application.service.spec.ts 业务项目删除时独立画布稳定对象引用保留，以及既有项目名称 CAS 与 viewer 权限回归（16/16）
- apps/backend/src/entrypoints/tests/phone-auth-dev-server.storage-upload.spec.ts 文件删除与独立画布引用保留回归（8/8）
- apps/backend/src/modules/storage/tests/upload-session.service.spec.ts 存储修复与持久保留根回归（9/9）
- apps/backend/src/entrypoints/tests/phone-auth-dev-server.upload-content.spec.ts 鉴权内容路由与生成入队重签回归（2/2）

- 上述测试已通过；最新独立 `canvasProjectId` 架构下统一 `new-canvas*.spec.mjs` 为 443/443，其中空画布快捷入口、双击落点与 LibTV 实测几何为 9/9、文件与资产为 61/61、类型化端口与节点标签为 14/14；`creator-api.spec.ts` 的历史检查点为 57/57。导演服务测试为 4/4，显式读取 `.env` 正式 `DATABASE_URL`，导演 HTTP 契约测试为 1/1，均使用 stub gateway 且未触发真实模型。工具预设后端服务为 4/4，画布视频合成与类型校验为 13/13，项目应用为 16/16，存储修复为 9/9，文件上传 HTTP 为 8/8；均读取 `.env` 正式 `DATABASE_URL`，未切换 `TEST_DATABASE_URL` 或备用端点。`npm run build:new-canvas` 与 `git diff --check` 通过；标题 CAS SQL 已以 `$6::text` 消除 PostgreSQL `42P08`。构建仍会报告既有 `project-detail.js` 重复对象键警告，本轮未修改该无关文件。

本轮图层完整成员与稳定对象引用的定向合并回归为 51/51；导航为 18/18、云保存为 41/41、生成为 69/69、文件与资产为 50/50、工具预设为 15/15、工作流队列最终为 18/18、类型化端口为 10/10、工作流边为 10/10、故事板为 9/9、媒体输入隔离为 5/5。该历史检查点的统一全量为 394/394；最新统一全量见后文最新检查点。

自定义字体运行时补强后，品牌套件专项为 6/6，当前并行工作树 `new-canvas*.spec.mjs` 为 341/341，墙钟约 3.62 秒；覆盖稳定对象路由、custom slot 注册、清理和异步切换防旧请求回写，`npm run build:new-canvas` 与定向 `git diff --check` 通过。

批量素材部分失败语义补强后，文件与资产专项为 37/37，当前并行工作树 `new-canvas*.spec.mjs` 为 345/345，墙钟约 3.68 秒；生产构建与定向 `git diff --check` 继续通过。

图片异步归档 stale 回写防护补强后，文件持久化专项为 15/15，当前并行工作树 `new-canvas*.spec.mjs` 为 346/346，墙钟约 3.65 秒；生产构建与定向 `git diff --check` 继续通过。

云素材/生成历史异步插入作用域门禁补强后，文件与资产专项为 38/38；`npm run build:new-canvas` 通过，仅保留既有 `project-detail.js` 重复对象键警告。云素材和生成历史的异步读取会在资源层仍打开、画布 API 与 `canvasProjectId` 未更换时才允许写入，原生图片后台归档也会在回写前确认当前画布、文件二进制和本地待归档节点仍有效；关闭资源层、切换画布或删除/重绑节点后，迟到结果会被丢弃且不跨作用域重试。当前并行工作树统一 `new-canvas*.spec.mjs` 为 352/352 全量通过。

工作流/故事板视图隔离补强后，故事板与主画布定向回归为 30/30，当前并行工作树 `new-canvas*.spec.mjs` 为 346/346，墙钟约 3.79 秒；`npm run build:new-canvas` 与 `git diff --check` 通过，构建仅保留既有 `project-detail.js` 重复对象键警告。

视频合成 HTTP 端到端测试覆盖密码登录、创建画布、保存节点和边、下载已归档图片、生成真实 MP4、ffprobe 确认 H.264、上传、运行历史、同键成功重放不重复上传、同键请求冲突、跨用户素材拒绝、终态失败重放、数据库 finalization 故障注入和云对象清理。本轮首次复跑时正式 `.env` 的 PostgreSQL `DATABASE_URL` 在创建数据库阶段短暂出现 `Connection terminated unexpectedly`，没有切换到 `TEST_DATABASE_URL` 或备用数据库；连接恢复后按最终代码重新运行并通过 1 项。

视频合成输出补强后的定向回归：前端生成 71/71、工作流节点 11/11、工作流边 13/13；后端画布规范校验 6/6、视频合成服务 7/7、工具预设服务 4/4。旧云文档的 `outputs[]` 仍按原 `loomicElement` 只读 hydration，端口字段不会被信任；只有用户显式保存或真实编辑触发重新序列化时才写入当前 `out_video` 契约，单纯打开旧文档不宣称已完成云端迁移。

视频合成无连接空态已按 LibTV 登录态直接证据收口为“空空如也，请连接视频节点后操作”，移除本地自行补充的第二行说明；视频合成与工作流节点定向回归为 28/28。该对齐仅涉及可见文案，不据此推断 LibTV 的内部端口 ID、数据类型或下游协议。

视频合成对象保留根已闭合：活动独立画布当前文档和全部 revision 中的 `resultStorageObjectId`，以及未删除的 `creator_canvas_node_artifacts.storage_object_id`，都会阻止存储修复或其他业务数据删除误删对应对象；原 `storageObjectId`、画布归属和已删除画布边界保持不变。正式 `.env DATABASE_URL` 下的定向回归覆盖仅节点结果、仅 artifact、当前文档和历史 revision。

早期历史检查点：云持久化专项 39 项、文件与资产专项 35 项、生成专项 53 项、文件持久化专项 13 项、标题同步专项 7 项和工作流恢复专项 12 项当时均通过。登录成功固定刷新页面，恢复键集合会随组件重建；持久化的 `running + taskId` 节点只轮询原任务。若节点本地缺少 taskId，则只查询其最高服务端 run 并严格核对输入，不调用新任务提交接口，也不回退旧 run。画布记录后端数据库测试包含取消历史、零伪产物、节点取消状态和并发运行编号断言；当前统一数字以后文最新检查点和各专项最新值为准。

独立 Chromium 已同时打开 LibTV 与本地新画布，完成节点菜单、工作流模板、工具布局、图层/资产侧栏、画布导航和快捷键面板对比。浏览器中已验证模板菜单的四种真实拓扑、资产面板搜索/类型/文件夹控制、聚合生成历史的全部节点、媒体分类、时间排序与批量入口；图层实际拖拽改变 z-order 后自动保存，刷新仍保持新顺序，验证结束后已恢复审计画布原顺序。留档截图：

最新构建已在独立 Chromium 中验证视频合成节点菜单、输入端口、分辨率、帧率、图片默认时长和逐片段时长控件。未归档输入会明确禁用合成；从云素材或生成历史再次插入的已归档图片现在保留 `storageObjectId` 并可直接参与合成。当时开发栈监听 `http://127.0.0.1:4310`；`/api/auth/session`、独立画布、revision 和 generation config 接口均使用正式数据库。generation config 已迁移为 `cosyvoice-v2`、必填官方音色、20000 文本上限及动态 schema 参数。音频节点卡片和参数面板保持可执行，但为避免费用未提交真实付费音频任务。

独立画布项目已完成 Chromium 实机联调：通过 `canvasProjectId` 新建、保存、重命名、复制和切换画布，临时 B 画布写入文本节点后，副本可加载同一节点；切回 A 画布时节点为空，刷新后仍为空，确认文档与本地状态按具体画布隔离。旧 `canvasId` 与业务项目绑定流程已经由后续重构移除，本段只保留独立画布隔离结论。

2026-07-21 早期历史检查点曾在独立 headed 测试浏览器保持当时可访问的指定 LibTV 画布；当时 `browse status` 为 `healthy / headed`、PID `10692`，标签页标题和 URL 与目标项目一致。该会话的登录态 DOM snapshot 确认中央“添加节点、移动、工具箱、素材库、角色库、历史记录、快捷键、教程”，左下除首位资产管理外的其余入口为“整理画布、小地图、连线可见、网格吸附、缩放”，以及左侧“画布/资产/资产管理”均为真实可交互元素。当前账户现已对该旧项目返回“没有权限访问此项目”，因此该段只保留为历史证据，完整顺序以后文当前可访问画布的直接证据为准。

本轮对“输入已更新”双动作、失败提示关闭/重试和历史失败 run 重试完成了代码与定向测试验证；headed 浏览器按单项超过 3 分钟即标记该轮测试完成并继续下一项的规则执行，但现有数据中没有供应商自然失败任务，因此不把真实失败任务 UI 记为 headed 实机通过。为避免产生费用，本轮未点击生成或历史失败重试，没有触发任何真实付费生成。

本轮同时核对历史删除对象 `453767ce-2653-469f-857b-b9d8c85826f6` 与上传会话 `834b6adf-78dd-48d7-aef8-a1ad73e8113b`：正式云库的 canvas document/revision 没有保存该 `uploadSessionId`，但 headed 浏览器的本地画布仍保留旧节点。加载时状态预检确认 `failed/deleted`，节点被标记为 `needs-file`，文件面板实际出现“重新选择源文件”。使用原 PNG 原位重绑后，新会话 `23fc08df-4845-4847-8580-1eac927b0769` 当时曾在刷新后返回 `200 / 261918B`；末轮审计发现该对象随后又被旧 `delete_failed` repair 重试路径删除，当前已是 `failed/deleted`，不能再把早先截图作为当前可用证据。当前修复会在 repair 重试前重新检查 durable roots、活动画布当前/历史引用和节点 artifact，防止未来复发，但不会伪造恢复已经删除的对象字节；确定终态预检、标记、立即保存和原位重绑由 3/3 专项及 316/316 全量套件覆盖。

登录态实机进一步确认：LibTV 中央“素材库”首先打开仅含“风格库 / 特效库”的小菜单；风格库包含“风格广场 / 我的收藏 / 最近使用”三标签、搜索、推荐与分类、“仅看可商用”和风格卡片，特效库同样包含三标签、搜索、推荐和特效卡片。左下“资产管理”是与中央“素材库”分离的独立入口，不再把二者视为同一层。角色库提供角色预设和“应用至画布”，历史记录提供图片/视频/音频分类、独立缩放、时间排序和批量操作；小地图和连线显隐可逆，移动入口提供“移动(V)/抓手工具(H)”。网格吸附、缩放菜单和自动布局在当前图上的内部状态证据有限，按三次观测上限标记本轮审计完成但不据此宣称完全一致；所有可逆开关与自动布局审计均已恢复原状态，未修改 LibTV 项目内容。

2026-07-21 已登录只读审计进一步确认 LibTV 工具箱：实际为视口居中的无蒙层 `480×460` 浮层，与底部工具栏间距约 8px；“我的工具箱”有 25 张三列可滚动 workflow 卡片，卡片约 `138×175`、可拖拽，悬停显示“使用”。列表协议为 `POST /api/canvas/workflow/list`，详情为 `GET /api/canvas/workflow/detail?workflowUuid=...`，拖拽 MIME 为 `application/x-liblibtv-canvas-workflow`；插入时会重生成全部 node/edge ID，并重写 parent/child/params 中的节点引用。另有 5 张“周星驰经典名场面”专用效果模板，它们依赖 LibTV 私有 `star-video2` 节点，不是普通 workflow，本项目尚无真实供应商执行能力，因此不以同名静态模板伪装支持。新画布最终实测工具箱为 `480×460`、间距 `7.97px`、三列独立滚动，预设卡约 `141×173`，支持分类状态保持、点击使用、拖放插入和用户/团队自定义工具。headed 浏览器在 `4312` 正式后端实际完成选中子图保存、`POST 201`、刷新持久化、点击插入、不同 `data-node-id`、创建 `v2`、选择历史 `v1` 后重命名仍保留 `v1`、复制和删除；测试数据与插入节点随后全部清理，刷新后画布恢复为空，控制台无错误，未触发生成。

2026-07-21 最终 split build 已在同一 headed 浏览器的 `4312` 标签刷新：资产页可见“上传素材”“批量操作”、类型与来源筛选及真实个人素材列表；上传和批量按钮的作用域、部分失败、顺序、分类事务与稳定下载路由由 `45/45` 专项覆盖。本轮没有实际上传用户文件或下载云资产，也没有触发付费生成；临时本地标签验证后关闭，独立浏览器恢复指定 LibTV 为唯一前台标签。

本地此前在独立 headed 静态构建中验证了 `CanvasFilesPanel` 大尺寸 dialog 的 `1120×760` 几何、完整遮罩、`aria-modal=true`、Escape 关闭以及角色库/历史记录的 requestId 视图切换；这只证明本地大层布局和切换代码，不证明中央“素材库”已与 LibTV 对齐，也不证明已登录后端素材上传成功。左侧紧凑文件/节点入口继续保留。

2026-07-21 当前登录态直接审计纠正了此前的入口归属判断：中央“素材库”是“风格库 / 特效库”小菜单；风格库真实结构为“风格广场 / 我的收藏 / 最近使用”、搜索、推荐与分类、“仅看可商用”和卡片列表，特效库真实结构为同样的三标签、搜索、推荐和卡片列表。`artifacts/libtv-cdp-central-assets-2026-07-20.png` 与 `artifacts/libtv-asset-manager-baseline.png` 显示的是独立资产管理界面及其“+新建”菜单，只可作为底部“资产管理”的历史证据，不能再作为中央“素材库”的对照证据。本地已把中央“素材库”从资产管理 dialog 改为同名小菜单；风格项进入复用真实风格目录和提示词应用链的专用 dialog，特效项在缺少真实执行契约时保持禁用。此前 headed 静态构建中的资产管理菜单展开、文本节点插入、“从生成历史选择”、两种视口可达性和零控制台错误仍只证明本地资产管理代码链和静态交互，不构成已登录后端上传实机完成。静态服务未连接已登录后端，本轮未选择用户文件、未实际上传、未触发模型生成；文件与资产专项当前为 52/52。

2026-07-21 本轮以 `artifacts/libtv-cdp-central-history-clean-2026-07-20.png` 为直接对照证据：LibTV 弹层标题为“历史资产”，没有通用文件视图标签或节点选择器，只保留“图片历史/视频历史/音频历史”、时间排序、批量操作，以及右上角“− 100% +”。本地中央“历史记录”入口已改为相同专用结构并聚合全部生成节点；左侧紧凑“生成历史”不受影响。缩放默认显示 `100%`，只通过历史产物容器 CSS 变量调整卡片密度，不读取或写入画布 `appState.zoom`；本地安全边界与步进属于实现约束，未冒充 LibTV 实测数值。定向文件与资产测试为 47/47、统一新画布测试为 389/389。本轮没有新增本地已登录 headed 点击或截图证据，因此不把源码和自动化测试写成实机验证。

2026-07-21 本轮以 `artifacts/libtv-cdp-central-character-library-2026-07-20.png` 为直接对照证据：LibTV 中央弹层标题为“角色库”，上方显示当前角色详情，底部提供角色缩略图选择带，并有“应用至画布”。本地中央“角色库”已收口为相同信息层级，当前项和缩略图带均来自真实角色资源，应用动作仍进入现有稳定对象插入链，不创建图片生成或工具节点；本地数据契约没有多视图、标签和最近使用字段，因此不复制同一预览或用静态数据伪造。本轮新增结论由源码与 48/48 文件资产自动化测试覆盖，尚未新增本地 headed 点击或截图证据。

2026-07-21 当前可访问的已登录 LibTV 画布 DOM 再次确认左下完整核心顺序：资产面板关闭态的首项为“资产管理”图标加文字，后接整理画布、小地图、连接线、网格吸附与缩放；留档 `artifacts/libtv-cdp-left-asset-management-clean-2026-07-20.png` 也显示相同关闭态结构。本地当时把原纯图标“生成文件”入口改为首位的“资产管理”图标加文字，并将已有自动整理、小地图、连接线、网格吸附四个同级按钮按该直接证据重排；事件、状态、快捷键和本地扩展控件均未改写。导航专项为 19/19。展开态随后由 2026-07-23 的同会话截图补证并继续收口。原先指定的旧 LibTV 项目链接在当前账户下返回“没有权限访问此项目”，因此本轮不把该旧项目写成可交互验证；当前证据只用于 LibTV 通用画布导航结构。

2026-07-21 当前可访问的已登录 LibTV 画布 DOM 与 `artifacts/libtv-canvas-baseline.png` 同时证明其顶栏并列显示项目名称 `textbox` 和“画布 1”切换器。本地在后续重构中保留视觉信息层级，但采用独立画布标题和画布项目选择：标题通过 `expectedTitle` 原子比较保护并发改名，pending、冲突恢复和切换前保存均以 `canvasProjectId` 隔离，不调用业务项目详情或项目更新 API。本段不把 LibTV 的内部业务归属推断为本地契约。

2026-07-21 保存通知回归复核曾稳定复现 `Cannot access 'closed2' before initialization`：订阅函数内的局部 `closed` 遮蔽 hub 关闭状态并触发暂时性死区，使 presence/revision 在首次订阅前失败。局部退订标记改名后，live collaboration 服务 3/3、鉴权 SSE HTTP 2/2 均通过，覆盖同画布隔离、关闭清理、跨 hub revision、传输回声去重、鉴权和最新 current-head；这仍不替代两台物理设备的生产验证，也不扩展为 CRDT/远端光标能力。

2026-07-21 快捷键、导航与保存再次在同一独立 headed 浏览器对照已登录 LibTV：LibTV DOM 明确显示成组/合并/解组/连线/复制/生成/适合屏幕均使用主修饰键，缩放菜单为百分比输入、放大/缩小/适合屏幕和 50/100/800%。当时本地刷新后“添加节点”显示 `Tab`、小地图默认关闭、缩放菜单命令与预设完整；当时版本的快捷键面板四栏仍包含 `V/H` 和手势提示，已由 2026-07-22 的直接截图复核结果取代。在创作助手输入框聚焦时发送 `Ctrl+S` 后页面未出现浏览器保存对话框，应用仍保持画布页并执行保存请求。最终构建在真实 Excalidraw 画布获得焦点后发送 `Ctrl+=`，底栏从 100% 变为 110%；随后 `Ctrl+-` 恢复 100%，控制台无错误。页面外壳或输入框持有焦点时不劫持缩放按键，该焦点边界与文本编辑保护一致。

2026-07-21 LibTV 拖动复制完成真实可撤销审计：`Option+拖动` 复制已有上游连接的“高清”节点后仍为 8 条边，`Cmd+Option+拖动` 后新增一条上游到副本的边并变为 9 条；两次均 `Cmd+Z` 恢复 13 节点/8 边。bundle 同时确认单节点以 `copyUpstreamEdges`、多选以 `copyInternalEdges` 读取主修饰键。本地受控复制的策略、ID/位置、双向绑定、锁定和阈值已由定向与全量测试覆盖；使用 CDP 发送本地指针时连普通无修饰拖动也未命中节点，因此该次 CDP 尝试按 3 分钟规则记为测试不成立，不作为本地实机通过证据。

2026-07-21 本地 headed 页面已验证中央“教程”按钮和四项菜单真实渲染：使用教程可用，客服/销售按公开配置状态启用，缺少独立真实配置的公众号禁用；页面控制台无新增错误。另在 `4312` 正式后端创建临时画布并用两个 headed 页面验证保存通知：页面 A 插入脚本生图后保存为 `serverRevision=2`，页面 B 自动出现同一两节点和一条类型化边；隐藏连接线并等待保存窗口后，服务端仍为 revision 2、edge 仍为 1、持久箭头 opacity 仍为 100，确认纯视图显隐不会制造 revision 或污染文档。随后恢复显隐并以 `DELETE 200` 删除临时画布；该验证证明同浏览器多页面通知和 revision 计数，不替代两台物理设备的生产验证。

2026-07-21 最终构建继续在同一独立 headed Chromium 的 `4312` 页面验证：底部“添加节点”打开后按 `Tab` 可关闭，再次打开并插入图片生成节点不会触发生成；从该选中节点直接切换到故事板时，工作流节点参数浮层会关闭，故事板卡片不再被遮挡，DOM 中也不再保留“提示词/模型/生成图片”编辑控件。验证后返回工作流，使用画布全选删除、`Ctrl+S` 和刷新确认临时节点均已清理，控制台无错误。留档 `artifacts/new-canvas-storyboard-selection-clear-2026-07-21.png`；该截图只证明非付费故事板切换与浮层清理，不作为真实失败供应商任务证据。

2026-07-21 本轮收尾再次在独立 headed Chromium 的 `4312` 构建中验证故事板真实 DOM 隔离：`main.lm-canvas-shell` 的 `data-view-mode` 为 `storyboard`，故事板区域存在；工作流工具栏、添加节点、连接端口、图层、小地图、底栏以及节点/素材编辑浮层计数全部为 0。验证未触发生成，临时本地标签已关闭；浏览器最终为 `healthy / headed`，仅保留并选中用户已登录的目标 LibTV 画布标签。

2026-07-21 本轮在同一独立 headed Chromium 的 `4312` 最新构建中向故事板发送可取消的合成 drag、drop 与图片 paste 事件，三者均未被画布 `preventDefault` 接管；`data-drop-active` 为 `false`、导入遮罩计数为 0，清空后的网络记录中没有本地上传请求。验证后关闭临时标签、收起 LibTV 对照侧栏并恢复目标 LibTV 画布为唯一前台标签；浏览器状态继续为 `healthy / headed`。该验证不触发生成，也不替代真实付费任务或两台设备生产验证。

本轮再次直接使用 `.env` 启动完整开发栈时，Redis `REDIS_URL` 出现 `ENETUNREACH`、PostgreSQL `DATABASE_URL` 出现 `Connection terminated unexpectedly`，并伴随既有 `ERR_HTTP_HEADERS_SENT` 日志；最新一次复试中 `generation-outbox` 仍在正式 `DATABASE_URL` 上出现同一连接终止，栈状态变为不健康后已停止。未切换 `TEST_DATABASE_URL`、HTTP-only 模式或备用端点。因此新增代码有构建、单元/HTTP 测试和 LibTV 登录态只读证据，但没有把本轮本地静态页面 `200` 或尚未完成真实已登录后端 headed 闭环的素材上传流程误记为后端功能运行成功。

LibTV 音频节点也已完成登录态只读审计：文本上限为 50000，包含模型、音色、停顿、语气词、语速 `0.5-2`、声调 `-12-12`、音量 `0.01-10`、音高/强度/音色调节，以及无、空旷回音、礼堂广播、电话失真、电音等音效，并具有输入/输出端口。LibTV 视频合成节点在无连接时显示“空空如也，请连接视频节点后操作”，具有左右连接端口；本轮未修改 LibTV 项目内容。该直接证据不证明 LibTV 右侧端口的内部 ID、数据类型或下游执行协议，因此这些内容不写成 LibTV 实测结论。

截至 2026-07-23，原指定 LibTV `spaceId=3288583&projectId=cd472f23580c4523bbb75b8051482ed1` 仍明确返回“没有权限访问此项目”，不是登录拦截；因此不继续操作该项目。同账号可访问的 `spaceId=224879&projectId=cf39ecbc53a34beab3d68c3965ddd4df` 只用于核对通用画布交互，不冒充原项目私有状态；登录态 DOM 确认中央“添加节点、移动、工具箱、素材库、角色库、历史记录、快捷键、教程”和左下“资产管理、整理画布、小地图、隐藏节点连线、网格吸附、缩放 41%”，本轮保持只读，未修改 LibTV 项目。此前本地静态构建实测可从“添加节点”插入视频合成和视频节点，DOM 同时存在“视频合成：any 输入端口”和“视频合成：video 输出端口”，并完成端口点击连接；静态服务未接正式后端，点击运行返回“模型配置加载失败”，不记为真实合成或下游生成成功。

2026-07-22 当前可访问的已登录 LibTV 画布实机确认：点击底部“资产管理”后出现工作区面板，顶层为“画布 / 资产”两个标签和右侧“资产管理”图标命令，默认“画布”页显示画布元素、筛选与节点计数；切换“资产”后显示“个人 / Agent”、搜索和素材类型筛选。该证据直接否定了把底部入口改成一打开就进入独立资产大层的猜测。本地因此只调整已有面板的信息层级，没有新增资产 API、静态卡片或伪能力；文件与资产专项为 51/51。由于正式 `.env DATABASE_URL` 的完整本地开发栈仍无法健康运行，本轮未把源码和专项测试写成最新本地 headed 后端闭环。

2026-07-22 本轮继续以 `artifacts/libtv-assets-reference-2026-07-21.png`、`artifacts/libtv-cdp-left-asset-management-clean-2026-07-20.png` 和 `artifacts/libtv-add-node-menu.png` 为直接证据收口三处：左侧工作区只保留“画布 / 资产”两个标签，并以右侧书本图标命令进入现有资产管理大层；左下六项 LibTV 核心工具改为连续组，本地背景、图层、布局设置和工作流运行移到其后分隔区；“添加节点”按截图改为紧凑节点列表与“添加资源”分区，上传、生成历史和素材库均复用现有真实链路。定点菜单会根据视口空间决定素材子菜单方向，窄屏或短视口改为内嵌可滚动布局，移动端中央工具条可横向滚动；从 Agent 返回个人资产时清理搜索条件。统一新画布测试为 410/410，生产构建通过；构建仍只有既有 `project-detail.js` 重复键警告。该轮未新增本地已登录 headed 后端闭环，不把源码、截图对照和自动化测试表述为运行环境实机完成。

2026-07-22 继续逐项读取同一资产工作区截图后，本地紧凑面板移除了截图不存在的通用“资产管理 / 项数 / 关闭”标题栏；“画布”页从仅列媒体文件改为投影全部真实非连线画布节点，固定底栏提供截图同位的收起动作和节点总数；“个人 / Agent”改为分段控件，空资产状态与截图一致，紧凑面板不再重复专用资产大层的上传和批量操作。非媒体节点保持只读定位，已有媒体文件管理、专用资产大层和后端契约均未改写。`artifacts/libtv-shortcuts-2026-07-20.png` 显示快捷键总览的“移动画布”栏只有键盘、触控板、鼠标和整理画布，本地据此暂时从总览移除重复的 `V/H`，真实移动工具和原生快捷键不变；但 `artifacts/libtv-drag-duplicate-audit.png` 的同栏明确包含“移动 V / 抓手工具 H”，两份历史直接证据冲突，不能把前者写成唯一结构或据此推断条件切换。

同一轮生产构建经本机 Chrome 静态验收：桌面 `1440×900` 下紧凑资产面板没有通用标题栏，“个人 / Agent”分段、文件夹空状态、固定底栏和专用资产管理入口可操作，紧凑面板不存在上传或批量按钮；移动 `390×844` 下文档宽度保持 `390`，面板边界在视口内。验收发现并修复了移动端项目顶栏 `z-index: 45` 压住资产面板的问题，面板现为 `z-index: 50`，命中栈确认标签在顶栏之上。并行契约审查还修复真实 `text-node` 显示成通用图标、普通画布节点搜索“上传”被误命中的两处问题。文件与资产专项为 52/52，统一新画布测试为 411/411，生产构建通过；截图为 `artifacts/new-canvas-static-asset-panel-desktop-2026-07-22.png` 与 `artifacts/new-canvas-static-asset-panel-mobile-2026-07-22.png`。静态服务没有接正式后端，素材 API 返回失败并显示真实错误态，因此该验收只证明当前构建的前端结构、响应式层级和失败态，不冒充登录态上传闭环。

2026-07-22 继续按 `artifacts/libtv-cdp-minimap-toggle-2026-07-20.png` 与 `artifacts/libtv-cdp-auto-layout-undone-2026-07-20.png` 的直接证据收口导航：桌面小地图改为从选中按钮正上方展开，空间不足时自动切到下方避让；底栏动作名称统一为 LibTV 的“整理画布”；整理成功后显示“是否保留此次整理结果？ / 还原 / 保留”。还原只恢复整理修改的节点/连线几何并保留之后发生的非几何内容更新，同时恢复整理前视口；保留仅关闭确认。待确认时按钮、设置入口和 `Alt/Option+Shift+F` 不会覆盖快照，小地图入口也会暂时禁用，正常场景更新继续进入原保存链。异步整理完成时只把布局几何合并到当前画布的最新场景，保留计算期间发生的文案、样式和新增节点更新；若同批节点的位置、锁定、分组、容器、画框、旋转或绑定关系已被用户修改，或新增/既有连线在此期间绑定到待移动节点，则中止本次整理，切换画布后也不会把旧结果或失败提示回写到新画布。整理前视口在布局实际应用前采集，不会把计算期间用户主动完成的缩放或平移误当成待回滚状态。确认期间若受影响节点或连线的几何/结构发生变化，“还原”会拒绝生成新结构与旧坐标混合的场景，并提示先保留当前结果后重新整理；纯文案和样式更新仍可安全保留。视口适配使用同步非动画更新，避免“还原”后被迟到动画覆盖；确认层出现前会关闭小地图且入口保持禁用，两个浮层不会互盖。生产构建在本机 Chrome 的 `1440×900` 与 `390×844` 静态验收均无页面异常和文档横向溢出；两种尺寸的小地图均与触发按钮左边缘对齐、保持至少 `8px` 间隔且完整位于视口内，桌面确认层完整可见，移动确认层改为固定底部后边界完全位于视口内。统一新画布测试为 420/420，导航专项为 28/28，生产构建通过；构建仍只有既有 `project-detail.js` 重复键警告。该静态验收未连接正式后端，不扩写为登录态保存或上传闭环。

2026-07-22 继续按 `artifacts/libtv-toolbox-overview-2026-07-21.png` 与 `artifacts/libtv-shortcuts-2026-07-20.png` 的直接证据收口中央工具条和快捷键总览：八个 LibTV 核心入口严格调整为“添加节点 → 移动 → 工具箱 → 素材库 → 角色库 → 历史记录 → 快捷键 → 教程”；本地已有连接节点和绘图工具保留在教程后的独立扩展分隔区，不插入 LibTV 主序列。快捷键总览移除该截图不存在的通用标题栏，把“节点创建副本”改为截图原文“创建副本”，四个分栏和右上关闭按钮保持不变；`V/H` 是否应出现在总览仍受另一张直接截图的相反证据约束，未据此追加条件化逻辑。真实 `CanvasToolMenu` 组件在 `1440×900` 与 `390×844` 浏览器中完成 DOM 与截图复核；移动端复核发现工具条横向滚动容器会裁切其固定面板，随后把快捷键层挂载到画布根节点并保留外部点击判断，复测面板完整可见、文档宽度仍为 `390`。统一新画布测试为 420/420，生产构建通过；截图为 `artifacts/new-canvas-toolbar-shortcuts-desktop-2026-07-22.png` 与 `artifacts/new-canvas-toolbar-shortcuts-mobile-2026-07-22.png`。该组件验收不连接正式后端，不扩写为登录态保存或生成闭环。

2026-07-23 已在同一可控登录会话中分别切换“画布”与“资产”，`artifacts/libtv-current-canvas-tab-2026-07-23.png`、`artifacts/libtv-current-assets-tab-2026-07-23.png` 和 `artifacts/libtv-current-canvas-row-menu-2026-07-23.png` 已无歧义确认页签归属、画布元素列表、资产空态与图片行菜单；2026-07-22 的 `libtv-assets-workspace-logged-in-2026-07-22.png` 仅保留为历史截图，不再单独承担页签归属证据。`artifacts/libtv-asset-manager-layer-logged-in-2026-07-22.png` 仍直接确认资产管理大层的“个人资产库 / 可灵主体库”、搜索、批量操作、新建以及“全部 / 其它 / 人物 / 场景 / 物品 / 风格 / 音效”；`artifacts/libtv-add-node-script-menu-logged-in-2026-07-22.png` 仍直接确认“脚本”子菜单为“脚本 NEW / 脚本（旧版） Beta”。本轮未点击子菜单叶项、未创建节点、未上传资产，也未改动 LibTV 项目，因此两种脚本节点的创建结果和数据契约仍不据此推断。实机几何还确认：当左右侧栏同时打开时，快捷键层相对剩余画布区域居中，而底部工具条仍按视口居中；本地快捷键 portal 因此从视口级 `fixed` 改为画布根节点内的 `absolute`，并复用左栏打开时的 `280px` 可用区补偿，同时限制浮层宽度不侵入左栏，既避开工具条横向滚动裁切，也会随可用画布区域移动。

2026-07-22 继续按登录态工具箱和资产管理大层证据做最小对齐：工具箱主标题固定为“我的工具箱”，问号在悬停或键盘聚焦时显示 LibTV 同文案说明与“查看详细教程”，关闭动作仍位于主标题行；现有真实分类与“保存所选”没有删除，移到次级控制行，“全部”不再冒充第二个“我的工具箱”。帮助层使用可包含教程链接的非模态对话层语义，命中桥覆盖问号与浮层之间的视觉间距，并从标题左缘定位以避免窄屏被工具箱裁切。资产管理类型同步为“全部 / 其它 / 人物 / 场景 / 物品 / 风格 / 音效”，新增独立 `assetCategory` 保存真实语义并保持原 `type/category` 媒体字段不变；人物、场景、物品和风格只按已有返回字段确定分类，只有真实 `sound_effect/sfx/音效` 元数据才进入“音效”，普通语音、配乐和未知项保守归入“其它”，分类会随画布自定义数据持久化。统一新画布测试为 422/422，生产构建通过；构建仍只有既有 `project-detail.js` 重复键警告。本轮没有伪造“可灵主体库”、私有工具箱名称或严格音效数据，也未把自动化与源码检查扩写为最新本地已登录后端闭环。

2026-07-22 继续按登录态资产管理大层与低缩放画布截图收口两处：专用资产管理侧栏不再复用多类旧云来源，只显示已有真实读取契约的“个人资产库”；截图中的“可灵主体库”仍因没有数据契约而不伪造。图片、视频生成节点新增纯视图标签投影，在缩放至节点卡片内容难以辨认时仍显示真实节点标题及当前质量/分辨率、画幅、时长或由既有图片质量契约计算出的输出尺寸；该投影不写入元素、自定义数据、撤销栈、保存文档或供应商请求。素材与端口专项测试为 64/64，统一新画布测试为 423/423，生产构建与 `git diff --check` 通过；构建仍只有既有 `project-detail.js` 重复键警告。本轮未新增本地已登录 headed 后端闭环，也未触发上传或付费生成。

2026-07-22 继续按 `artifacts/libtv-accessible-canvas-logged-in-2026-07-22.png` 的直接证据收口节点连线显隐反馈：底栏控件使用稳定无障碍名称“节点连线”，悬停操作标题随状态切换为“隐藏节点连线 / 显示节点连线”，隐藏态继续保持选中；每次切换后在按钮邻近位置重新挂载可关闭的“点击可显示/隐藏画布上的连线”状态提示，并提供 `role="status"`、实时播报和独立关闭按钮。提示支持外部点击与 `Escape` 关闭，打开缩放等相邻底栏菜单也会先关闭提示，避免浮层重叠。原有显隐仍是纯视图投影，不写元素元数据、保存正文或撤销历史。最新生产构建在本地独立画布完成 `1440×900` 与 `390×844` 真实点击验收，实际 DOM 已覆盖连续切换、相邻菜单互斥、`Escape`、关闭按钮、状态标题和选中状态；桌面提示与 LibTV 锚点关系一致，移动端提示及关闭按钮完整位于视口内且文档宽度保持 `390`，测试创建的本地节点已清理。截图为 `artifacts/new-canvas-connection-notice-desktop-2026-07-22.png` 与 `artifacts/new-canvas-connection-notice-mobile-2026-07-22.png`。导航专项为 28/28，统一新画布测试为 423/423，生产构建通过；该静态服务未连接正式后端，不扩写为登录态云保存、上传或付费生成闭环。

2026-07-22 继续按 `artifacts/libtv-generation-audit-baseline-2026-07-21.png`、`artifacts/libtv-functional-reference-2026-07-19.png` 与 `artifacts/libtv-accessible-canvas-logged-in-2026-07-22.png` 的直接证据收口工作流端口：LibTV 在空闲低缩放画布和已有连线场景中不常驻端口圆点，本地因此只在节点被选中、连线模式开启、已选择输出端口或端口拖拽进行中显示端口；空闲端口保留 DOM 定位但设为不可见、不可点击、`tabIndex=-1` 与 `aria-hidden=true`，连接完成或取消后自动随状态收起。独立代码审查发现移动端 `pointercancel` 未退出连接模式，以及连接成功后焦点可能停留在刚隐藏的端口；两项均先补失败测试，再分别关闭拖拽状态并把端口焦点送回稳定的“连接节点”入口。该改动只消费现有选中与连线状态，不写元素、自定义数据、撤销栈、保存正文或生成请求。生产构建已在隔离 Chromium 实测：`25%` 空闲态 5/5 端口隐藏且选中后 5/5 恢复；新增真实视频节点后以脚本输出端口点击连接到视频输入端口，连接模式由 `true` 回到 `false`、9/9 端口收起、目标进入已连接态，焦点回到“连接节点”；`41%` 空闲态 9/9 收起；`390×844` 移动端刷新后同为 9/9 收起且文档宽度保持 `390`。截图为 `artifacts/new-canvas-port-idle-hidden-25pct-2026-07-22.png`、`artifacts/new-canvas-port-selected-25pct-2026-07-22.png`、`artifacts/new-canvas-port-idle-hidden-41pct-2026-07-22.png` 与 `artifacts/new-canvas-port-idle-hidden-mobile-2026-07-22.png`。类型化端口专项为 14/14，统一新画布测试为 427/427，生产构建与 `git diff --check` 通过；构建仍只有既有 `project-detail.js` 重复键警告。隔离 LibTV 会话当前访问画布 URL 会跳回首页并显示“注册/登录”，因此本轮没有把旧登录截图扩写为当前在线实测。

2026-07-22 继续按 `artifacts/libtv-functional-reference-2026-07-19.png` 与 `artifacts/libtv-accessible-canvas-logged-in-2026-07-22.png` 核对底部布局：LibTV 的中央工具条以整个视口为中心，不随右侧 Agent 面板或左侧工作区漂移。本地此前把工具条绝对定位在被创作助手压缩的画布区域内，并在左栏打开时额外偏移，导致 `1440×900` 下“缩放、背景、图层、运行”等底栏按钮被中央条盖住。当前中央条改为带全局 `--app-ui-scale` 补偿的视口固定居中；补偿严格从与全局缩放相同的 `769px` 断点启用，窄桌面且左栏打开时，含本地扩展的底栏响应式上移一行，宽屏、左栏关闭与移动布局保持原路径，按钮顺序和功能均未改动。隔离 Chromium 实测中央条中心为 `x=720/1440`，右侧助手开关前后不漂；左栏打开后底栏与中央条垂直分离，10/10 底栏按钮的中心命中自身；断点两侧分别为 `384/768`（无全局缩放）和 `384.5/769`（0.75 全局缩放），`390×844` 文档宽度保持 `390`。截图为 `artifacts/new-canvas-toolbar-centered-panel-open-2026-07-22.png`。导航专项为 29/29，统一新画布测试为 428/428，生产构建通过；构建仍只有既有 `project-detail.js` 重复键警告。

2026-07-23 登录态直接证据闭合了上一轮的页签歧义，并识别出无需私有后端即可安全落地的图片行操作差距：本地画布媒体行新增与 LibTV 一致的“…”入口，菜单前三项严格为“重命名 / 复制 / 下载”，复制复用现有 `duplicateCanvasMediaElement` 单次 `IMMEDIATELY` 更新，定位仍为独立直接动作；已有文件夹移动与删除保留在分隔后的本地管理区，不删除功能。代码复核发现行内绝对定位会被 `content-visibility` 与滚动容器裁切，随后把菜单改为 `document.body` Portal 的固定层，按触发器视觉矩形自动水平避让和垂直限制，并按 `body` 实际缩放换算坐标；Portal 显式同步画布主题变量、字体与盒模型，并补齐首项聚焦、上下方向键、Home/End、Tab 收起、`Escape` 回焦及外部点击收起。隔离 Chromium 在导入真实图片后确认菜单包含“重命名 / 复制 / 下载 / 未分类 / 从画布删除”，定位仍独立；`1280×720` 的 `body zoom=0.75`、断点两侧 `768×900`（`zoom=1`）与 `769×900`（`zoom=0.75`）均确认菜单使用固定定位且完整落在视口内。文件与资产专项为 55/55，统一 `new-canvas*.spec.mjs` 为 429/429，生产构建通过；构建仍只有既有 `project-detail.js` 重复对象键警告。本地独立静态页不接正式后端，因此不把归档失败扩写为登录态上传、下载或云保存闭环。

2026-07-23 继续按 `artifacts/libtv-functional-reference-2026-07-19.png`、`artifacts/libtv-generation-audit-baseline-2026-07-21.png` 与 `artifacts/libtv-current-canvas-tab-2026-07-23.png` 的稳定视觉证据收口中央主工具条：八项 LibTV 核心入口及本地连接/绘图扩展现在由单一深色圆角胶囊承载，不再以透明图标组直接浮在画布上；“添加节点”改为中性高对比实心按钮，不再随项目品牌强调色变化。按钮顺序、事件、Popover 锚点、视口固定居中和本地扩展分隔线均未改变。隔离 Chromium 实测 `1440×900` 中心为 `x=720`，左侧资产面板打开后仍为 `x=720` 且与底栏无垂直重叠；复核还发现中窄桌面上，画布根节点的绝对底栏会出界或与新增胶囊相交，因此 `761-1470px` 统一把底栏固定到视口并上移一行，侧栏打开时再按剩余宽度提供横向滚动。`768px` 侧栏关闭与打开均保留 `5px` 垂直间距，关闭时 10/10 按钮中心命中自身；`1024px`、`1470px` 间距为 `3.25px`，`1471px` 恢复 LibTV 同排布局且水平重叠为 `0`。断点两侧工具条中心分别为 `384/768`（`body zoom=1`）和 `384.5/769`（`body zoom=0.75`）；`390×844` 文档宽度保持 `390`，工具条完整落在视口内并可横向滚动到末项“绘图工具”。截图为 `artifacts/new-canvas-tool-capsule-desktop-2026-07-23.png`、`artifacts/new-canvas-tool-capsule-mobile-2026-07-23.png` 与 `artifacts/new-canvas-tool-capsule-panel-768-2026-07-23.png`。导航专项为 30/30，统一 `new-canvas*.spec.mjs` 为 430/430，生产构建通过；构建仍只有既有 `project-detail.js` 重复对象键警告。该浏览器验收使用独立静态页，不扩写为后端保存、上传或生成闭环。

2026-07-23 继续按资产入口两态的直接证据收口：`artifacts/libtv-cdp-left-asset-management-clean-2026-07-20.png` 证明桌面关闭态为“图标 + 资产管理”，`artifacts/libtv-current-canvas-tab-2026-07-23.png`、`artifacts/libtv-current-assets-tab-2026-07-23.png` 与 `artifacts/libtv-current-canvas-row-menu-2026-07-23.png` 证明左栏展开后首项收为纯图标。本地现在仅在 `min-width: 761px` 的展开态隐藏文字并收为同组 `28×28`，关闭态继续显示文字；tooltip、`aria-label` 与 `aria-pressed` 同步表达“打开/关闭资产管理”。浏览器复核发现工作台全局按钮 padding 曾把其余底栏按钮撑宽，已由底栏自身 `padding: 0` 恢复声明尺寸；同时修正 `761-1470px` 固定底栏的左侧栏偏移，由既有内联面板宽度叠加 `--workbench-rail-width + 0.45rem` 的 margin 补偿定位，不再藏在面板下，也不新增 `!important`。隔离 Chromium 实测 `1280×720`（`body zoom=0.75`）展开态按钮为视觉 `21×21`、面板右侧留 `12px`；`768×900` 为 `28×28`、留 `16px`，`769×900` 为视觉 `21×21`、留 `12px`，按钮中心均可命中，底栏无页面级横向溢出。`390×844` 保留“资产管理”文字，按钮中心仍可命中，文档宽度保持 `390`，不把桌面截图外推为 LibTV 移动端行为。对抗审查还发现全局 `Tab` 新建节点快捷键会拦截底栏按钮的正常焦点遍历；现在仅在事件目标自身为按钮、链接、表单控件、ARIA 交互角色或可聚焦元素时忽略该快捷键，既不把 Excalidraw 画布的可聚焦祖先误判为控件，也不抢走真实控件的焦点导航。浏览器实测从资产入口按 `Tab` 会移动到下一底栏按钮且不会打开新增节点菜单；以画布 `<canvas>` 为目标时仍会阻止默认事件并打开新增节点菜单。截图为 `artifacts/new-canvas-asset-entry-closed-2026-07-23.png` 与 `artifacts/new-canvas-asset-entry-open-2026-07-23.png`。导航专项为 31/31，统一 `new-canvas*.spec.mjs` 为 431/431，生产构建通过；构建仍只有既有 `project-detail.js` 重复对象键警告。该静态页不接正式后端，不扩写为上传或云保存闭环。

2026-07-23 继续修复图层与保存的两个内部交互断链：图层面板在普通图层或图层组重命名输入中按 `Escape` 时，不再由 document capture 先关闭面板；独立 Chromium 对两种重命名均确认输入退出、名称未提交、面板保持打开，留档 `artifacts/new-canvas-layer-rename-escape-2026-07-23.png`。顶栏“保存失败，点击重试”现在同时覆盖正文和画布标题失败；正文重试事件按当前 `canvasProjectId` 隔离，显式重试与 `Ctrl/Cmd+S` 共用会取消旧计时器的当前场景保存路径，避免同一 pending 内容被旧自动重试再次排队。定向保存/图层/云持久化回归为 88/88，统一 `new-canvas*.spec.mjs` 为 434/434，生产构建通过；构建仍只有既有 `project-detail.js` 重复对象键警告。图层证据来自不接后端的静态生产构建；正文真实云故障与点击恢复本轮仍由状态机和接线自动化覆盖，不扩写为已登录后端故障注入闭环。

2026-07-23 使用本地生产构建、密码登录和项目 `.env` 的正式后端连接完成真实素材闭环：在上传前同时打开同一独立画布的两个 Chromium 标签，选择仓库内真实 PNG 后依次收到上传会话创建、对象 blob 写入与完成归档的 `200`，归档结果带稳定 `storageObjectId`；`Ctrl/Cmd+S` 后服务端画布达到 revision 2，正文只保留鉴权稳定内容地址，不含 `data:` 或 `blob:`。第二标签未刷新即从远端 revision 看到同名素材，刷新后仍可恢复，鉴权内容路由先返回 `307` 再取得非空 `image/*`。请求审计确认未发起生成类 POST；截图为 `artifacts/new-canvas-real-upload-2026-07-23.png`。临时独立画布随后通过画布项目删除接口清理，画布与对象内容路由均验证为 `404`，本地 HTTP-only 服务已退出。该闭环证明当前单机双标签的真实上传、保存、实时采纳与刷新恢复，不外推为两台物理设备或 revision 并发冲突验证。

2026-07-23 继续按 `artifacts/libtv-asset-manager-layer-logged-in-2026-07-22.png` 与 `artifacts/libtv-asset-manager-baseline.png` 的专用资产管理层直接证据收口桌面宽度：仅 `.is-assets-dialog` 在桌面使用带全局 UI 缩放补偿的约 `90vw`，历史记录、角色库和风格库等通用 dialog 继续保留既有 `1120px` 上限。独立生产构建实测 `2048×1152` 下资产层占视口 `89.35%` 并居中；`900×900` 与 `390×844` 均完整位于视口内且无页面级横向溢出，截图为 `artifacts/new-canvas-assets-dialog-wide-2026-07-23.png`。同批修复把底栏菜单、`Ctrl/Cmd+0` 与整理后聚焦统一到可见元素集合，并在 ELK 异步计算期间把节点显隐变化纳入结构冲突检测，避免迟到布局覆盖用户刚完成的隐藏操作。统一 `new-canvas*.spec.mjs` 为 435/435，生产构建通过；构建仍只有既有 `project-detail.js` 重复对象键警告。

2026-07-23 继续闭合旋转节点的导航与整理边界：新增共享轴对齐可视包围盒计算，小地图和自动整理不再直接使用未旋转的 `x/y/width/height`；矩形、图片、文本、框架及当前工作流矩形节点按旋转后的精确外包框参与投影与间距，负宽高元素继续以 Excalidraw 的有符号尺寸确定旋转中心。TDD 先复现了 90° 的 `300×50` 节点仍投影为横向、纵向整理发生 `130px` 可视重叠，以及负宽度中心漂移，再由共享计算闭合。独立 Chromium 在默认横向整理下确认两个 90° 节点的小地图宽高比为 `1:6`、可视间距为 `120px`，截图为 `artifacts/new-canvas-rotation-aabb-2026-07-23.png`；静态服务器缺失的请求均严格限于 `/api/`，没有前端资源或运行时异常。导航专项为 35/35，统一 `new-canvas*.spec.mjs` 为 438/438，生产构建通过；构建仍只有既有 `project-detail.js` 重复对象键警告。椭圆、菱形等非矩形图形当前使用保守外包框，不宣称为形状轮廓的最小精确包围盒。

2026-07-23 继续按 `artifacts/libtv-current-canvas-tab-2026-07-23.png` 中“画布元素”旁的可见排序入口收口资产工作区：新增前景在前/背景在前的显示排序切换，默认仍保持现有前景在前列表，切换时只反转筛选后的展示数组，不调用 `updateScene`，因此不会改变场景 z-order、撤销栈或保存正文。TDD 先稳定复现缺少排序投影与入口，再补齐纯函数和接线；独立 Chromium 以三个真实 Excalidraw 矩形确认列表由“前景/中间/背景”精确反转为“背景/中间/前景”，IndexedDB 中元素顺序始终为 `qa-back/qa-middle/qa-front`，截图为 `artifacts/new-canvas-canvas-order-toggle-2026-07-23.png`。文件与资产专项为 57/57，统一 `new-canvas*.spec.mjs` 为 440/440，生产构建通过；构建仍只有既有 `project-detail.js` 重复对象键警告。LibTV 截图只证明排序入口可见，本地的方向标签和纯显示边界属于可回归的安全实现，不冒充其内部排序字段或持久化契约。

2026-07-23 继续闭合中央移动工具在窄屏的可达性：原二级菜单作为工具条内绝对定位子项，在 `390×844` 横向滚动工具条启用 `overflow-x: auto` 后会被纵向裁切。当前菜单改为挂载到 `.loomic-canvas-root` 的 Portal 固定层，仍以移动按钮为首选锚点，并根据实际菜单尺寸、视口边界及 `body zoom` 做水平与垂直夹取；Portal 点击纳入原外部点击判定。独立 Chromium 实测菜单矩形为 `left=8 / top=691 / right=172 / bottom=781`，完整位于 `390×844` 视口内；打开时聚焦当前选项，方向键可切换，`Escape` 回到“移动”，`Tab` 转到下一工具，点击 H 后菜单回写抓手选中态，画布交互后按 V 再回写移动选中态，截图为 `artifacts/new-canvas-movement-menu-mobile-2026-07-23.png`。新增纯函数覆盖底部、左上与右下三类视口边界，并收紧 Portal 容器的源码回归断言；统一 `new-canvas*.spec.mjs` 为 441/441，生产构建通过，构建仍只有既有 `project-detail.js` 重复对象键警告。快捷键总览继续保持现状：`artifacts/libtv-shortcuts-2026-07-20.png` 与 `artifacts/libtv-drag-duplicate-audit.png` 对 `V/H` 是否列入总览的直接证据相互冲突，在同一在线会话复核变体条件前不改面板内容。

2026-07-23 继续闭合专用资产管理“新建”菜单的本地嵌套弹层正确性：打开后聚焦首个可用真实动作，支持 `ArrowUp/ArrowDown` 循环、`Home/End` 跳转；`Escape` 只关闭子菜单并把焦点还给“新建”，不会再被外层 document capture 提前解释为关闭整个资产管理；`Tab/Shift+Tab` 关闭菜单后分别进入“搜索资产”和“批量操作”。关闭资产管理或离开专用视图时同步清理菜单状态与全局监听，重开不恢复幽灵菜单；执行菜单项后也会在当前层仍存在时安全回焦。TDD 先使缺失的索引与键盘动作决策红灯，再由纯行为函数覆盖 Escape、Tab、Shift+Tab、方向键和非处理键，并把源码接线断言限定在该菜单自己的 handler 与生命周期守卫内，避免跨无关 Escape 分支误命中；文件与资产专项为 58/58，统一 `new-canvas*.spec.mjs` 为 442/442，生产构建通过。独立 Chromium 另行实测首项“文本”→方向键“图片”→End“从生成历史选择”、Escape 保留大层、Tab/Shift+Tab 焦点落点及关闭重开状态，截图为 `artifacts/new-canvas-asset-create-menu-keyboard-2026-07-23.png`。静态页控制台错误仅来自预期缺失的 `/api/*`，未出现前端资源或运行时异常。现有 LibTV 截图只直接证明资产层与同构“添加节点/添加资源”菜单外观同时可见，不能证明该菜单的触发序列或键盘规则；因此上述键盘语义仅记录为本地可访问性与嵌套弹层修复，不冒充已验证的 LibTV 内部行为。

2026-07-24 继续复核 `artifacts/libtv-current-canvas-row-menu-2026-07-23.png` 已直接证明的图片行“重命名 / 复制 / 下载”三点菜单结构，并闭合本地行菜单的真实键盘焦点断链。原实现对 `Tab/Shift+Tab` 一律 `preventDefault` 后回到“更多操作”，形成软焦点陷阱；改为纯决策函数后，`Tab` 退出到后置“定位”，`Shift+Tab` 退出到前置行主按钮，`Escape` 仍回到“更多操作”，方向键与 `Home/End` 保持原菜单内导航。独立 Chromium 随后发现 Portal 获得焦点时原行失去 `:focus-within`，行操作组会被 `visibility:hidden`，导致正确的焦点目标仍无法落入；当前以菜单打开态保持行操作可见，并在关闭状态提交前同步完成焦点转移。忙碌中的插入、重绑或资产动作会同步禁用“更多操作”，避免把焦点送往已禁用的行主按钮。TDD 先分别复现错误 Tab 决策、忙碌态入口和 Portal 显隐冲突，再把源码断言限定在 `FileRow` 区段；文件与资产专项为 58/58，统一 `new-canvas*.spec.mjs` 为 442/442，生产构建通过，仍只有既有 `project-detail.js` 重复对象键警告。独立 Chromium 导入真实 PNG 后实测首开焦点“重命名”、Tab→“定位”、Shift+Tab→行主按钮、Escape→“更多操作”、End→“从画布删除”，页面运行时错误为 0，截图为 `artifacts/new-canvas-row-menu-keyboard-2026-07-24.png`。LibTV 截图仍只证明可见菜单结构，不证明其键盘规则；本轮键盘语义记录为本地可访问性修复，不冒充 LibTV 内部行为实测。

2026-07-24 继续闭合专用资产管理、历史资产、角色库和风格库四类大层的模态焦点边界。当前构建先稳定复现角色库最后一个控件按 `Tab` 后落到背景“由鹿流创建”，随后 `Escape` 关闭仍停在背景链接；根因是大层虽然声明 `aria-modal=true`，根节点只在打开时获焦，既没有首末环绕，也没有保存打开入口。当前新增纯 Tab 决策：根节点分别进入首项或末项，首项 `Shift+Tab` 与末项 `Tab` 环绕，零控件留在 dialog 根，普通左侧 panel 不接管；真实可聚焦元素在每次按键时重新读取，动态禁用和显隐变化不会沿用过期列表。回焦来源在 `openFilesView` 同步调用时保存，Escape、右上关闭和遮罩三条关闭路径共用同一恢复链；原入口仍连接时返回原控件，素材子菜单或紧凑资产面板入口已经卸载时分别回到中央“素材库”或底栏“打开资产管理”，资产层内切换到历史层不会覆盖最初入口。资产“新建”子菜单继续优先拥有自己的 capture 键盘语义，未引入全局焦点库或大范围 `inert`。TDD 先得到 57/59 的预期红灯，再达到文件与资产专项 59/59；统一 `new-canvas*.spec.mjs` 为 443/443，生产构建通过，仍只有既有 `project-detail.js` 重复对象键警告。独立 Chromium 对四种大层逐一从真实首末控件验证双向环绕，并分别用 Escape、关闭按钮、遮罩关闭；角色库回“角色库”、历史资产回“历史记录”、资产管理回“打开资产管理”、风格库回“素材库”，资产→历史的层内切换关闭后仍回最初资产入口，页面运行时错误为 0，截图为 `artifacts/new-canvas-dialog-focus-loop-2026-07-24.png`。现有 LibTV 证据只约束这些大层的结构与入口层级，不证明其内部键盘规则；本轮继续按本地模态可访问性闭环记录，不冒充 LibTV 键盘实测。

2026-07-24 代码复核继续发现上述焦点环绕在动态控件卸载时仍有缺口：若当前聚焦的“插入所选”等控件因退出批量态被 React 移除，浏览器会把焦点退回 `body`，原先挂在 dialog 根节点的冒泡监听收不到下一次 `Tab`，背景页面仍可被穿透。当前把同一纯 Tab 决策接到仅在专用 dialog 打开期间存在的 document capture 监听，每次按键继续实时读取可见、可用控件；处理后的事件同时停止后续传播，资产“新建”子菜单既有 window capture 仍先处理自己的 `Tab/Shift+Tab/Escape`，普通左侧资产 panel 不注册焦点锁。TDD 先以“焦点已落在 dialog 外、下一次 Tab 必须重新收回且卸载监听可清理”的失败回归稳定复现，再达到文件与资产专项 60/60；按最新独立 `canvasProjectId` 架构集成后的统一 `new-canvas*.spec.mjs` 为 443/443，生产构建通过，仍只有既有 `project-detail.js` 重复对象键警告。独立 Chromium 真实创建、聚焦并移除 dialog 内临时控件后确认 `document.activeElement === body`，随后 `Tab` 回到“关闭角色库”、`Shift+Tab` 回到末项“重新加载资源库”；资产“新建”子菜单的 Tab→“搜索资产”、Shift+Tab→“批量操作”、Escape→“新建”保持不变，普通侧栏末项按 Tab 会继续离开侧栏。静态页控制台仅有预期缺失的 `/api/*` 请求，不存在此次改动引入的前端运行时异常；该修复记录为本地模态可访问性闭环，不冒充 LibTV 内部键盘行为证据。

2026-07-24 提交前终审只在本轮画布边界内补齐七项回归：删除项目或素材版本前先将画布产物从资产、版本外键安全脱钩，同时保留仍被独立画布引用的稳定存储对象；视频合成网络响应不确定时以原始请求快照和原幂等键恢复，不用之后修改过的参数冒充同一请求；双击快捷添加、素材拖放与弹层定位统一补偿工作台 `body zoom`，旋转节点和线性元素按实际几何命中；专用大层内的行菜单 portal 保持自己的键盘焦点；`Ctrl/Cmd+S` 先提交尚未失焦的项目标题；Excalidraw 根容器获得焦点时仍允许画布 `Tab` 快捷键。统一前端回归为 443/443；后端受影响测试合计 50/50，均显式读取当前 `.env` 的正式 `DATABASE_URL`。这些结论来自自动化测试和代码审查，未扩写为新增 LibTV headed 实机证据。

- `artifacts/libtv-live-2026-07-20.png`
- `artifacts/new-canvas-live-2026-07-20.png`
- `artifacts/libtv-shortcuts-2026-07-20.png`
- `artifacts/new-canvas-shortcuts-2026-07-20-v2.png`
- `artifacts/new-canvas-workflow-toolbox-2026-07-20.png`
- `artifacts/libtv-final-reference-2026-07-20.png`
- `artifacts/new-canvas-assets-final-2026-07-20.png`
- `artifacts/libtv-audio-composition-audit-2026-07-20.png`
- `artifacts/libtv-canvas-baseline.png`
- `artifacts/libtv-add-node-menu.png`
- `artifacts/libtv-cdp-central-assets-2026-07-20.png`
- `artifacts/libtv-cdp-central-character-library-2026-07-20.png`
- `artifacts/libtv-cdp-central-history-clean-2026-07-20.png`
- `artifacts/libtv-cdp-toolbox-clean-2026-07-20.png`
- `artifacts/libtv-toolbox-panel-readonly-2026-07-21.png`
- `artifacts/new-canvas-toolbox-parity-final-2026-07-21.png`
- `artifacts/new-canvas-assets-dialog-final-2026-07-21.png`
- `artifacts/new-canvas-shortcuts-navigation-save-final-2026-07-21.png`
- `artifacts/libtv-drag-duplicate-audit.png`
- `artifacts/new-canvas-help-menu-drag-connection-2026-07-21.png`
- `artifacts/qa-core-image-cost-preflight-2026-07-21.png`
- `artifacts/qa-core-expired-upload-recovery-2026-07-21.png`
- `artifacts/qa-core-rebound-upload-reload-2026-07-21.png`
- `artifacts/libtv-cdp-minimap-toggle-2026-07-20.png`
- `artifacts/libtv-cdp-auto-layout-applied-2026-07-20.png`
- `artifacts/libtv-cdp-auto-layout-undone-2026-07-20.png`
- `artifacts/new-canvas-video-composition-unarchived-2026-07-20.png`
- `artifacts/new-canvas-multicanvas-cosyvoice-v2-2026-07-20.png`
- `artifacts/libtv-toolbox-overview-2026-07-21.png`
- `artifacts/libtv-toolbox-template-help-2026-07-21.png`
- `artifacts/libtv-toolbox-zhouxingchi-2026-07-21.png`
- `artifacts/new-canvas-tool-presets-4312-open.png`
- `artifacts/new-canvas-user-tool-preset-insert-4312.png`
- `artifacts/new-canvas-user-tool-preset-final-4312.png`
- `artifacts/new-canvas-storyboard-selection-clear-2026-07-21.png`
- `artifacts/new-canvas-static-asset-panel-desktop-2026-07-22.png`
- `artifacts/new-canvas-static-asset-panel-mobile-2026-07-22.png`
- `artifacts/new-canvas-static-minimap-desktop-2026-07-22.png`
- `artifacts/new-canvas-static-minimap-mobile-2026-07-22.png`
- `artifacts/new-canvas-static-layout-review-desktop-2026-07-22.png`
- `artifacts/new-canvas-static-layout-review-mobile-2026-07-22.png`
- `artifacts/new-canvas-toolbar-shortcuts-desktop-2026-07-22.png`
- `artifacts/new-canvas-toolbar-shortcuts-mobile-2026-07-22.png`
- `artifacts/libtv-specified-canvas-logged-in-2026-07-22.png`
- `artifacts/libtv-accessible-canvas-logged-in-2026-07-22.png`
- `artifacts/libtv-assets-workspace-logged-in-2026-07-22.png`
- `artifacts/libtv-asset-manager-layer-logged-in-2026-07-22.png`
- `artifacts/libtv-add-node-script-menu-logged-in-2026-07-22.png`
- `artifacts/new-canvas-connection-notice-desktop-2026-07-22.png`
- `artifacts/new-canvas-connection-notice-mobile-2026-07-22.png`
- `artifacts/new-canvas-generator-labels-41pct-2026-07-22.png`
- `artifacts/new-canvas-generator-input-updated-41pct-2026-07-22.png`
- `artifacts/new-canvas-compact-asset-filters-desktop-2026-07-22.png`
- `artifacts/new-canvas-compact-asset-filters-mobile-2026-07-22.png`
- `artifacts/new-canvas-port-idle-hidden-25pct-2026-07-22.png`
- `artifacts/new-canvas-port-selected-25pct-2026-07-22.png`
- `artifacts/new-canvas-port-idle-hidden-41pct-2026-07-22.png`
- `artifacts/new-canvas-port-idle-hidden-mobile-2026-07-22.png`
- `artifacts/libtv-session-redirected-home-2026-07-22.png`
- `artifacts/libtv-current-canvas-baseline-2026-07-23.png`
- `artifacts/libtv-current-canvas-tab-2026-07-23.png`
- `artifacts/libtv-current-assets-tab-2026-07-23.png`
- `artifacts/libtv-current-canvas-row-menu-2026-07-23.png`
- `artifacts/new-canvas-tool-capsule-desktop-2026-07-23.png`
- `artifacts/new-canvas-tool-capsule-mobile-2026-07-23.png`
- `artifacts/new-canvas-tool-capsule-panel-768-2026-07-23.png`
- `artifacts/new-canvas-asset-entry-closed-2026-07-23.png`
- `artifacts/new-canvas-asset-entry-open-2026-07-23.png`
- `artifacts/new-canvas-minimap-hidden-filter-2026-07-23.png`
- `artifacts/new-canvas-real-upload-2026-07-23.png`
- `artifacts/new-canvas-assets-dialog-wide-2026-07-23.png`
- `artifacts/new-canvas-rotation-aabb-2026-07-23.png`
- `artifacts/new-canvas-canvas-order-toggle-2026-07-23.png`
- `artifacts/new-canvas-movement-menu-mobile-2026-07-23.png`
- `artifacts/new-canvas-asset-create-menu-keyboard-2026-07-23.png`
- `artifacts/new-canvas-row-menu-keyboard-2026-07-24.png`
- `artifacts/new-canvas-dialog-focus-loop-2026-07-24.png`

`artifacts/new-canvas-audio-ready-2026-07-20.png` 生成于本轮 CosyVoice V2 最终契约确认前，不再作为当前音频节点契约的有效验证证据。`artifacts/qa-core-rebound-upload-reload-2026-07-21.png` 仅记录重绑对象被旧 repair 路径再次删除前的历史瞬时成功，不作为当前对象可用证据。`artifacts/libtv-cdp-central-assets-2026-07-20.png`、`artifacts/libtv-asset-manager-baseline.png` 和 `artifacts/new-canvas-assets-dialog-final-2026-07-21.png` 只保留为资产管理或本地旧版通用素材 dialog 的历史证据，不证明当前中央“素材库”结构或真实后端上传成功。多画布联调截图记录的是临时画布清理后的默认画布和已保存状态；同一 Chromium 会话中另行核对了 `CosyVoice V2`、`longxiaochun_v2`、20000 文本上限及官方参数范围。未点击生成，未产生付费请求。

## 尚未闭合的能力

- 底部资产工作区的无标题栏紧凑结构、“画布 / 资产”、右侧资产管理图标命令、完整画布节点投影与计数、“个人 / Agent”分段控件、资产页搜索/单一筛选图标、空状态，以及图片行“重命名 / 复制 / 下载”三点菜单已按 2026-07-23 同一登录会话的直接 DOM 与三张截图完成对齐；画布页继续使用自己的“全部”类型筛选与搜索入口。专用资产管理类型也已按真实语义映射为“全部 / 其它 / 人物 / 场景 / 物品 / 风格 / 音效”，且不破坏原媒体类型字段；专用大层的来源侧栏已收敛为当前具备真实读取契约的“个人资产库”，不恢复旧业务项目或剧集素材来源。专用资产管理大层的桌面宽度已按两份 LibTV 截图收口为约 `90vw`，并保持中窄屏视口内可达；通用 dialog 的 `1120px` 上限未被扩大。真实本地文件上传、归档、保存、同浏览器第二标签接收和刷新恢复闭环已经完成；仍未闭合的是非媒体节点各自的更多菜单内容、LibTV 的“可灵主体库”数据源、风格资源 API 与个人资产的统一分页、音频中“音效”的严格子类型，以及 LibTV 上传是否只入库、搜索后批量选择是否清空。当前项目没有这些可验证契约，因此不增加同名假入口、伪动作或静态数据。
- LibTV“添加节点”的“脚本”下级菜单已由登录态直接证据闭合为“脚本 NEW / 脚本（旧版） Beta”，但两项的创建结果、默认参数、节点结构和旧版兼容语义仍未验证；本地继续使用既有真实脚本节点创建链，不把唯一现有节点擅自标成新版或旧版，也不新增无执行契约的静态菜单项。取得可安全验证的数据契约后再对齐。
- 整理结果确认、还原/保留和小地图按钮锚点已经按直接截图闭合；小地图以及菜单、快捷键、整理后聚焦三条 fit 路径均已排除 `loomicHidden` 隐藏元素，异步整理期间的显隐变化也会阻止迟到结果覆盖当前场景。矩形类节点的小地图投影与自动整理已统一使用绕中心旋转后的轴对齐可视包围盒，90° 节点、负宽高中心和整理间距均由行为测试与独立 Chromium 闭合；椭圆、菱形等非矩形图形仍使用保守矩形外包框。尚未闭合的是自动整理当前仍会移动所有未删除的非箭头元素，自由绘图、装饰图形和 `loomicHidden` 隐藏元素仍会参与布局。后续需先取得 LibTV 对这些元素参与规则的直接行为证据，再调整筛选，不能凭推测改变用户画布。
- 中央“素材库”的入口归属已闭合为“风格库 / 特效库”小菜单，底部资产管理保持独立；尚未闭合的是 LibTV 风格库的“我的收藏 / 最近使用”、商用过滤与完整分类，以及整套特效库。当前风格 dialog 只展示本项目真实返回的官方/批量风格并应用到图片生成节点；特效项保持禁用。只有取得真实收藏/最近使用字段、商用标记、特效目录、可执行节点、模型计费、任务恢复和产物归档契约后才能继续对齐，不用静态卡片或普通云资产冒充。
- LibTV 专用角色库的基础大层呈现已闭合：专用角色详情、真实角色缩略图选择带和“应用至画布”均已接入，且只消费真实 `character` 资源。尚未闭合的是 LibTV 的多视图、标签和最近使用；本地资源契约当前没有这些字段，后续只能在真实返回数据和交互证据具备后对齐，不允许用静态角色、多图复制或伪“最近使用”补齐外观。本轮也未新增本地 headed 点击或截图验证。
- LibTV “周星驰经典名场面”的 5 个专用效果模板依赖其私有 `star-video2` 节点和供应商执行协议；本项目当前只完成通用规范 workflow 与用户/团队自定义工具预设，不提供同名专用效果。只有在模型目录、输入输出、计费、任务恢复和产物归档均取得真实可验证契约后，才能把这些模板列为已支持。
- LibTV 工具箱的“我的工具箱”、帮助问号、悬停/聚焦说明、教程链接和关闭按钮已按直接证据闭合；本地真实工具分类与“保存所选”保留在次级控制行，该控制行属于当前真实工具预设契约的本地扩展差异。仍未闭合的是截图中的当前私有模板名“周星驰经典名场面”及其 5 个专用效果，因为它们依赖项目尚无的 `star-video2` 执行、模型、计费、恢复和归档契约，不以静态标题或假模板冒充。
- LibTV 音频节点中的停顿、语气词、强度、音色调节和音效尚未找到 CosyVoice V2 HTTP Provider 可安全确认的正式字段，因此当前 UI 不展示、请求不透传，也不以静态选项冒充真实能力。LibTV 显示 50000 字及声调 `-12-12`、音量 `0.01-10`，百炼官方 V2 则限制 20000 字、声调 `0.5-2`、音量 `0-100`；本项目以真实供应商契约为准，保留这项可见差异。最小真实替代是继续使用纯文本标点、分句和换行表达停顿，并在 provider 正式发布对应结构化字段前保持这些控件明确阻断；不能把文本预处理包装成与 LibTV 同名的可精确调节能力。
- 通用 Agent 工具绑定和跨节点独立产物集合仍未完成。当前已完成的是可复用的导演 Agent 配置集合及其可执行导演节点，尚没有任意 Agent 类型、工具权限绑定、Agent 自有媒体集合或跨项目 Agent 运行空间，因此不把导演 Agent 资产扩展解释为完整通用 Agent 平台。
- 创作助手真正多模态仍未完成：运行时文本模型目录目前只有 `deepseek-chat` 与 `qwen-plus`，现有 OpenAI-compatible 文本网关没有经过验证的视觉模型或 multimodal adapter，且网关会持久化上游 request body。前端不提供图片附件入口，只发送选中元素的标题、文本和提示词；后端对任何非空 `attachments` 在调用模型前返回 `400 new_canvas_assistant_vision_unsupported`，图片节点也只作为文字摘要处理，不发送图片内容、素材 URL、`data:`、`blob:` 或 `storageObjectId`。存储层已有对象归属校验与短期签名 URL，但在视觉模型、MIME/大小限制和签名 URL 脱敏审计补齐前，不将其误接为“图片理解”能力。
- 自定义字体运行时已完成当前品牌套件主字体的画布内真实加载：`.ttf/.otf/.woff/.woff2` 通过 10 MB 正式上传策略归档并绑定稳定对象，画布从鉴权内容路由注册字体，应用后的文字保存 custom slot 与品牌字体资产 ID/名称。当前 Excalidraw 公共生产入口不提供多自定义字体注册表或导出字体嵌入 API，因此同一画布同时保留多套上传字体、切换品牌后保持旧字体独立渲染、PNG/SVG 导出嵌入和字体许可证校验仍未完成；品牌套件与独立画布的跨设备关联仍需按最新独立画布契约重新验证，不把旧业务项目绑定结论沿用到当前架构。
- 完整 Loomic 多人实时协作仍未完成。当前可访问的 LibTV 登录态 DOM 直接显示“正在跟随”，并提供 `aria-label="退出跟随"`、文案“取消 ESC”的退出动作；本地目前只有鉴权、按画布隔离的 SSE presence 与 revision 通知，revision 已使用 Redis pub/sub 跨实例广播。presence 仍为单服务进程内状态，协议没有远端视口、光标、选区、跟随目标或上行位置数据，前端采纳远端 revision 时还会保留本地滚动、缩放和选区；因此本轮只把 LibTV 跟随交互登记为未闭合差异，不添加“正在跟随/取消 ESC”静态假 UI，也不把 revision 通知/轮询误称为完整实时协作。
- 仍需补真实付费模型任务、跨刷新任务恢复、稳定 `storageObjectId` 素材跨物理设备和 revision 并发的生产端到端验证；单机双标签的真实上传、稳定对象 hydration、鉴权读取、远端 revision 采纳与刷新恢复已经闭合，但尚未在两台真实设备验证生产对象全链路。真实供应商自然失败任务的提示、关闭和历史重试也尚无 headed 样本；当前音频闭环已由专项、HTTP 和登录态 Chromium 的非付费路径覆盖，但未实际发起付费音频生成。所有后续验证都应以独立 `canvasProjectId` 为作用域，不再覆盖旧业务项目多画布路径。
- 视频合成使用随包 FFmpeg/ffprobe 二进制，其中 FFmpeg 为 GPLv3 且启用 libx264；正式分发前必须完成 GPLv3 源码提供、许可证通知、构建对应性等合规评审，不能只按普通 npm 依赖发布。

## 代码边界

- `apps/web/new-canvas/index.html`：独立页面入口。
- `apps/web/new-canvas/src/main.jsx`：React 页面装配、本地持久化和宿主操作。
- `apps/web/new-canvas/src/loomic-core`：Excalidraw 编辑器、工具栏、图层、底栏、媒体节点、工作流执行和生成状态管理。
- `apps/web/new-canvas/src/loomic-shell`：菜单、文件面板、项目名、空状态和创作助手侧栏。
- `apps/web/new-canvas/src/app.css`：宿主布局和响应式样式。
- `apps/web/tests/new-canvas*.spec.mjs`：入口、生成、连线、素材、图层、导航、云保存、版本历史与恢复回归测试。

## 构建与验证

```text
npm run build:new-canvas
node --test apps/web/tests/new-canvas*.spec.mjs
git diff --check
npm run dev:background:status
```

构建产物为 `apps/web/new-canvas/app.js`，页面由现有 Web 服务通过 `/new-canvas/` 提供。

## 下一阶段

取得原指定 LibTV 项目权限后继续只读核对视频合成下游行为；在此之前只使用当前可访问画布核对通用画布交互，不把其他项目状态冒充指定项目证据。其余优先项仍为真实付费音频任务与跨刷新恢复、真实供应商失败任务的 headed 交互验证、独立画布跨设备切换、稳定对象素材跨设备和 revision 并发的生产端到端验证；待供应商正式契约确认后，再实现 LibTV 的停顿、语气词、强度、音色调节、音效参数和 `star-video2` 类专用视频效果。多人实时协作、多自定义字体并存与导出嵌入、通用 Agent 工具绑定和 Agent 自有产物集合保持独立阶段。视频合成随包 FFmpeg/libx264 的 GPLv3 发布合规必须在正式分发前完成。
